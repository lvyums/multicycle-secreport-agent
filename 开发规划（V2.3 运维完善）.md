# 多周期网安报告智能体 — V2.3 运维完善（第 6 轮）

编制：2026-08-07 ｜ 范围：可观测性 / 推送真实化 / 审计补全 / 前端路由权限
依据：V2.1 生产评估 🟡 项（V2.2 已消 🔴 上线硬门槛）

## 0. 现状基线

- 344 用例全绿，覆盖率 94%（4148 stmts）；V2.2 已交付登录锁定/强制改密/任务恢复/并发上限/部署形态/备份
- 已有：TraceID 请求日志、AuditLog（操作/状态变更/LOGIN_FAIL/CHANGE_PWD）、PushLog（stub 推送记录）、后端 RBAC 403 兜底
- 缺口：/health 仅静态 200；无指标端点；推送是 stub 不真发；导出无审计；前端路由无角色拦截；审计无展示页

## A 可观测性（零新依赖）

- A1 /health 就绪探针：检查 DB 连接（SQLite/MySQL）+ 缓存（Redis ping 或 memory 恒过）+ 向量目录可写；返回 `{status: ok|degraded, checks:{db, cache, vector}, ts}`；依赖全挂才 503
- A2 /metrics Prometheus 文本端点（手工拼文本，零依赖）：`sec_report_task_total{status=}` / `sec_report_task_failed_total` / `sec_report_llm_calls_total{mode=llm|fallback}` / `sec_report_push_total{result=}` / `sec_report_task_duration_seconds{cycle=}` 计数 + 请求总数
- A3 指标埋点：report 状态机（report_service 状态变更处）+ llm_factory 调用/降级处 + push 发送处，统一 `infra/metrics.py` 原子计数（threading.Lock）

## B 推送真实化（能力层，mock 默认保测试）

- B1 push/webhook_strategies.py 增加 real 模式：钉钉（HMAC-SHA256 时间戳+secret 签名）、企微（key 直发）、邮件（stub 保留标注"未实现"）；配置 `push.mode=mock|real`（settings.push_mode）
- B2 失败重试 2 次指数退避（0.3*3^n，复用数据源重试模式）+ PushLog 记录真实 HTTP 状态码/响应体摘要
- 验证约束：外网受限无法真实联调 → real 模式用 monkeypatch 假 HTTP 单测；浏览器/mock 联调保持 stub 路径

## C 审计补全

- C1 导出审计：report_export_service 导出 md/docx 时写 AuditLog（EXPORT_REPORT, operator, target=ReportVersion, format）
- C2 登录失败/改密审计已有（V2.2 LOGIN_FAIL / CHANGE_PWD）
- C3 前端审计页 /audit：admin 可见，表格（TraceID/操作者/动作/目标/详情/时间）+ 动作筛选 + 分页；MainLayout 菜单加"审计日志"

## D 前端路由级角色拦截

- D1 router meta.roles（admin/analyst/viewer），beforeEach 校验 + 无权限跳 /403 占位页（后端 403 兜底已存在，前端防页面壳）
- D2 菜单显隐已有（V2.0 isAdmin/isAnalyst），本次只补路由级

## E 测试与验收（test_v23）

- test_observability.py：/health 依赖明细（mock db 故障 → degraded）、/metrics 文本格式（含 # TYPE/HELP 与计数变化）
- test_push_real.py：real 模式钉钉签名正确性（已知 secret 断言签名）、企微发送、失败重试 2 次、PushLog 记录状态码；mock 模式回归
- test_export_audit.py：导出 md/docx 后 AuditLog 有 EXPORT_REPORT 且 operator/format 正确
- 验收：全量回归 344+ 全绿、覆盖率 ≥94%；浏览器实测审计页 + viewer 直输 /audit 被拦
- 部署说明：docs/deploy/prometheus.md（采集配置示例）+ README 版本历史补 V2.3

## 风险与回滚

- 推送 real 模式生产才启用（settings.push_mode=real + webhook 地址/密钥 env 注入）；误发风险=先 mock 验收再切 real
- /metrics 无鉴权（内网 Prometheus 拉取）；若需公网暴露需反代加白名单（nginx 示例注明）
- 全程零新依赖；如失败仅影响新增端点，回滚=撤销 commit
