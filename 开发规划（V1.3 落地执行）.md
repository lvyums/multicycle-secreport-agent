# 多周期网安报告智能体 — V1.3 落地执行规划（第 3 轮）

编制：2026-08-06  |  范围：前端可视化零代码（数据源管理 / 知识库 / 报告选配 / 任务日志）

## 0. 现状盘点（V1.2 完成，V1.3 基础已具备）

| 能力 | 现状 | V1.3 动作 |
|---|---|---|
| 数据源 | DataSourceConfig 表 6 类配置；API 只有 list/test | 补 CRUD（create/update/toggle/delete）+ 前端管理页 |
| 知识库 | 无知识文档表；judge 有 rag_refs 字段但无注入源 | 新增 KnowledgeDoc 表 + CRUD API + 研判注入 |
| 报告选配 | 章节固定（模板写死六章）；推送手动触发 | ReportConfig 单例表（章节开关/周期/渠道/自动生成）+ 渲染裁剪 + 自动推送 |
| 任务日志 | ReportTask 已有 duration_ms/data_source_stats/error_msg | 补 logs API + 前端任务日志页 |
| 前端 | 4 页（看板/历史/预览/调度）+ 预览页审核/推送/对比 | 新增 4 页（数据源/知识库/报告选配/任务日志）+ 菜单 |

## M 阶段 — 数据源管理（零代码配置）

- M1: DataSourceConfigRepo 补 create/update/toggle/delete（按 name 查重）
- M2: datasource API 补 POST /create /update /toggle /delete；类型元数据 GET /meta（类型→配置表单字段）
- M3: DataSources.vue — 列表（类型徽标/状态开关/同步策略）+ 新增/编辑对话框（类型下拉→动态表单）+ 测试连接 + 删除确认

## N 阶段 — 知识库

- N1: KnowledgeDoc 实体（id/title/category/content/enabled/created_at/updated_at）+ Repo CRUD + /api/kb API
- N2: Knowledge.vue — 文档列表 + 分类筛选 + 新增/编辑对话框 + 启停开关
- N3: report_service 研判前读取启用知识文档 → 注入 prompt_builder 的参考段（rag_refs，V1.3 全量注入摘要，V2.0 再上向量检索）

## O 阶段 — 报告选配

- O1: ReportConfig 实体（id=1 单例：enabled_cycles 列表 / sections 开关 dict / push_channels 列表 / auto_generate 布尔）+ /api/config/report GET/Save
- O2: report_service 生成时按配置裁剪 sections（渲染前过滤开关关闭的章节）+ 生成后 auto_generate 时自动推送所有已选渠道
- O3: ReportConfig.vue — 周期多选 / 章节开关组 / 推送渠道多选 / 自动生成开关

## P 阶段 — 任务日志

- P1: /api/report/logs（分页 + 状态筛选 + 周期筛选）+ detail 补 data_source_stats 展开
- P2: TaskLogs.vue — 任务列表（ID/周期/窗口/状态/耗时/触发来源）+ 展开行显示数据源统计与错误

## 验收

- pytest 补用例：数据源 Repo CRUD / 知识库 CRUD / 章节裁剪 / 配置读写（目标 60+ 用例）
- 浏览器链路：4 新页加载 + 数据源启停/新增 + 知识库增删 + 报告选配保存 + 任务日志展开
- 全量回归：五周期生成 + 审核 + 对比不受影响
- git 提交（每阶段一个 commit）
