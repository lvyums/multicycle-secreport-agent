# 多周期网安报告智能体 — V2.2 上线硬门槛（第 5 轮）

编制：2026-08-06 ｜ 范围：安全凭据 / 任务恢复 / 部署形态 / 数据备份
依据：V2.1 生产就绪评估（代码实查）——🔴 必须项全部纳入本版；🟡 可观测性/推送真实化/审计补全排 V2.3

## 0. 现状基线（代码实查）

- pytest 338 用例全绿，覆盖率 94%（4062 stmts）；测试隔离 SQLite + mock 数据源
- 认证：pbkdf2 密码哈希 + hmac 自签 token 12h；**SECRET_KEY 默认值固定 `sec-report-dev-secret-change-me`**（auth_service.py:11），不设 env 即可伪造 token
- 种子账号 admin/analyst/viewer 默认密码 admin123/analyst123/viewer123，无强制改密
- 无登录失败锁定；无登录失败审计
- 报告生成为进程内 asyncio 任务（run_background）：**服务重启 → RUNNING 任务永久卡死**，无恢复机制
- 调度器自研 asyncio 单进程：多副本部署会重复触发
- CORS 全放开 `allow_origins=["*"]`（main.py:68，注释明确"开发期"）
- 应用无 Dockerfile / 进程守护 / HTTPS；报告文件落本地 reports/
- 无数据库备份策略

## 部署目标形态（V2.2 结论）

```
用户浏览器
   │ HTTPS
Nginx 反代（TLS 终结 + 静态资源 / API 分流）
   │ 127.0.0.1:8001
uvicorn 单进程（systemd 守护，自动拉起）   ← V2.2 定版：单进程
   ├── MySQL 8（数据源/任务/版本/用户/审计）
   ├── Redis 7（缓存，可选；登录锁定计数优先落 DB 免依赖）
   └── 本地卷（reports/ 报告文件 + vector_data/）
```

**为什么单进程**：run_background 后台任务与自研调度器都是进程内状态，多 worker/多副本会导致任务重复执行、调度重复触发。V2.2 用 systemd 单实例 + 自动重启；多副本（需分布式锁 + 任务恢复增强）留 V2.3。

---

## A 阶段 — 安全凭据加固

- **A1 SECRET_KEY 强校验**：config/settings.py 增加 `SECRET_KEY` 字段；启动时若为默认值且 `APP_ENV=production` → 拒绝启动并提示生成命令（`openssl rand -hex 32`）。开发环境保持默认值可用（不阻断本地开发）
- **A2 首次登录强制改密**：
  - User 实体加 `must_change_pwd` 字段（种子用户初始 True，之后新建用户默认 False 或按管理员指定）
  - 登录响应与 `/api/auth/me` 返回 `mustChangePwd`；前端检测到 → 强制跳转改密页（不允许跳其他路由）
  - 新增 `POST /api/auth/change-pwd {oldPwd, newPwd}`：校验旧密码 + 新密码强度（≥8 位含字母数字）；成功后清 must_change_pwd、使旧 token 失效（前端清 localStorage 重新登录）
  - RBAC：改密接口 require_login 即可（自己改自己）；管理员可重置他人密码（现有 reset-pwd 保留）
- **A3 登录失败锁定**：
  - auth_service 增加失败计数：同用户名连续失败 5 次 → 锁定 15 分钟（`login_fail_count` + `locked_until` 存 User 表，免 Redis 依赖；Redis 可用时也可切）
  - 锁定期间登录返回 423 语义（ApiResponse 业务码，message 提示剩余时间）
  - 登录成功清零计数；锁定解除自动恢复
  - **登录失败写 AuditLog**（operator=尝试的用户名、action=LOGIN_FAIL、detail=来源 IP）
- **A4 改密后会话失效**：前端改密成功后清 token 强制重新登录；后端不做 token 吊销（自签无状态，A2 已覆盖主要场景）

## B 阶段 — 任务恢复与并发控制

- **B1 启动任务恢复**：main.py startup 事件扫描 `status IN (PENDING, RUNNING)` 的任务：
  - 配置项 `RECOVER_ON_STARTUP=true`（默认开）：PENDING/RUNNING → 重置 FAILED，reason 写"服务重启中断，请重跑"；管理员可见该原因（任务详情/日志）
  - 幂等：只处理当前进程启动前的遗留任务；恢复动作落 AuditLog
- **B2 调度器单实例约束**：settings 增加 `SCHEDULER_ENABLED`；文档明确多副本部署时仅主副本开启调度（V2.3 再上分布式锁）
- **B3 并发生成控制**：`MAX_CONCURRENT_GENERATION=2`（settings 可配）：submit 时若 RUNNING 任务 ≥ 上限 → 返回 1001 业务码"生成任务已达上限，请稍后"；防多报告同时跑打爆 LLM 配额/拖垮 MySQL

## C 阶段 — 部署形态

- **C1 CORS 白名单配置化**：settings 增加 `CORS_ORIGINS`（逗号分隔）；`APP_ENV=production` 且未配置 → 拒绝启动；dev 保持 `*`。main.py 从 settings 读取
- **C2 后端 Dockerfile**（sec_report_agent/Dockerfile）：
  - 多阶段构建：python:3.10-slim 基础 + requirements 安装（chromadb 体积大，用 `--no-cache-dir`）
  - 非 root 用户运行（uid 10001）；EXPOSE 8001；HEALTHCHECK 打 `/health`
  - `.dockerignore`：排除 __pycache__/tests/data/vector_data/*.db
  - docker-compose.yml 增加 `app` 服务（depends_on mysql/redis，env 注入 SECRET_KEY/APP_ENV）
- **C3 进程守护（生产首选 systemd 而非容器）**：docs/deploy/sec-report-agent.service 示例：`ExecStart=uvicorn main:app --host 127.0.0.1 --port 8001` + `Restart=always` + `EnvironmentFile=/etc/sec-report-agent/env`（SECRET_KEY/APP_ENV/DATABASE_URL 集中管理，不进 git）
- **C4 Nginx 反代 + HTTPS**：docs/deploy/nginx.conf 示例：TLS 终结（证书路径占位）、`/` → 前端静态 dist 或 5174、`/api` `/health` → 127.0.0.1:8001、限流 `limit_req`（防爆破，配合 A3）

## D 阶段 — 数据备份

- **D1 备份脚本** `scripts/dev/backup.sh`：
  - MySQL：`mysqldump --single-transaction` → `backups/sec_report_<date>.sql.gz`
  - 文件：`reports/` + `vector_data/` + `.env`（脱敏提示）tar.gz
  - 保留策略：保留最近 7 天，可 `--keep N` 覆盖
  - cron 示例注释（每日 02:00）
- **D2 恢复说明**：docs/deploy/restore.md：恢复 MySQL dump + 解包文件卷 + 重启；验证步骤（登录/历史报告列表/预览）

## E 阶段 — 测试与验收

- **E1 新增测试**（tests/test_v22/）：
  - 登录锁定：5 次失败 → 锁定；锁定期间登录被拒；成功登录清零；AuditLog 落 LOGIN_FAIL
  - 强制改密：must_change 用户登录后 me 返回标记；change-pwd 成功清标记；旧密码校验失败 400；弱密码 400
  - 任务恢复：造 PENDING/RUNNING 任务 → 调 recover → FAILED + reason；非遗留任务不受影响
  - 并发上限：RUNNING 达上限 → submit 返回 1001
  - CORS：production 配置下跨域 Origin 被拒（TestClient 带 Origin 头）
  - SECRET_KEY 强校验：APP_ENV=production + 默认 key → 启动抛错
- **E2 验收清单**（全部浏览器/命令行实测）：
  - [ ] 种子账号首次登录 → 强制跳改密 → 改密后重登成功；旧密码登录失败
  - [ ] 连续 5 次错密码 → 提示锁定 15 分钟；审计日志有 LOGIN_FAIL 记录
  - [ ] kill 后端进程（任务 RUNNING 中）→ 重启 → 任务变 FAILED 带"服务重启中断"原因
  - [ ] 同时提交 3 个生成 → 第 3 个收到"已达上限"提示
  - [ ] production CORS：跨域请求被拒；dev 正常
  - [ ] systemd + Nginx 起服务 → HTTPS 访问 /docs 与前端正常
  - [ ] backup.sh 执行 → backups/ 出现归档；restore 流程验证一次
- **E3 回归**：全量 pytest 保持 338+ 用例全绿、覆盖率 ≥94%

## 范围外（V2.3 候选）

- 可观测性：Prometheus /metrics + 告警、结构化日志收集（Loki/ELK）、/health 依赖检查
- 推送真实化：钉钉/企微真实 webhook + 签名 + 重试；邮件
- 审计补全：导出/下载留痕页面；前端路由级角色拦截
- 多副本：分布式锁 + 调度主备 + 任务恢复增强
- PDF 导出、多租户、模板品牌定制

## 风险与回滚

- 强制改密若用户忘记旧密码 → 管理员 reset-pwd 兜底（已有接口）
- 任务恢复把 RUNNING 重置 FAILED 可能误伤正常任务 → 恢复只处理"进程启动前已存在"的任务（按 start_time < 进程启动时间过滤），且可配置关闭
- 并发上限过低影响体验 → 可配，默认 2，按环境调整
- CORS/SECRET_KEY 生产强校验可能阻塞部署 → 报错信息给出明确修复指引，不留模糊错误
