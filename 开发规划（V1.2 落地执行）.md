# 多周期网安报告智能体 — V1.2 落地执行规划（第 2 轮）

编制：2026-08-06  |  依据：迭代开发规划.md V1.2 范围 + 落地执行版.md 技术栈/约定
范围：全周期（日/周/季/年）+ 全数据源（Excel/情报/历史报告）+ 审核推送 + 版本对比回溯

## 0. 现状盘点（V1.0 已完成，V1.2 地基已具备）

| 能力 | 现状 | V1.2 动作 |
|---|---|---|
| 五周期调度 | calc_window 已支持 DAILY/WEEKLY/MONTHLY/QUARTERLY/YEARLY | 无需改 |
| 指标聚合 | MetricAggregator(cycle) 参数化 | 无需改 |
| 模板 | 仅 monthly_report.md.j2；template/ 下 daily/weekly/quarterly/yearly 目录已建空 | 补 4 个周期模板 + 按周期选模板 |
| mock 数据 | 仅 2026-06~07 月窗口（syslog/api/vuln 三文件） | 生成器参数化全年，覆盖五周期窗口 |
| 数据源 | Syslog/API/DB 三类适配器 + 工厂 | 加 Excel/情报/历史报告 3 类 |
| 版本 | ReportVersion.status 字段已存在（默认 DRAFT） | 补审核状态机 + 审核 API |
| 推送 | PushLog.channel 已支持 local/email/dingtalk/wecom；仅 local 策略 | 补 dingtalk/wecom/email stub 策略 |
| 对比 | MetricSnapshot.metrics_json 已存（对比数据基础） | 补版本对比服务 + API + 前端页 |
| 依赖 | openpyxl 3.1.5 已装（Excel 适配器可用） | 零新增联网依赖 |

## 1. 阶段分解（E/F/G/H + 前端 + 验收）

### E 阶段 — 全周期模板与渲染
- E1 模板：template/{daily,weekly,quarterly,yearly}/<cycle>_report.md.j2
  - 日/周报精简版：概述/告警/TOP/趋势/建议（五章节，无漏洞台账章节可留空自动隐藏）
  - 季/年报扩展版：概述/告警/漏洞/攻击/趋势(含环比)/建议（复用月报结构 + 对比段）
- E2 渲染器按周期选模板：MdRenderer._template_name = f"{cycle}_report.md.j2"（目录按 cycle）
- E3 mock 生成器参数化：mock_data_gen 支持 `--year 2026` 全年数据（syslog/api/vuln 覆盖 1~12 月，
  日报窗口=昨天、季报窗口=上季度、年报=上一年均有数据）；数据量可控（月度 ~1500 条）
- E4 验证：五周期各跑一次 pipeline → SUCCESS + 模板渲染正确

### F 阶段 — 全数据源扩展（3 类新适配器）
- F1 Excel 适配器（openpyxl）：场景=威胁情报台账/资产清单导入；mock 生成 threat_intel.xlsx
- F2 情报源适配器：mock 生成 intel_iocs.jsonl（IOC 列表：IP/域名/哈希 + 置信度 + 来源）
- F3 历史报告源适配器：从 MetricSnapshot 回读上一周期指标 → trend.compare 环比数据
  （V1.1 里 compare 是空的，本阶段补齐——全周期报告的对比亮点）
- DataSourceType 枚举 + AdapterFactory 注册 + init_datasources.py 增加配置（幂等）

### G 阶段 — 审核推送
- G1 审核状态机：ReportVersion.status DRAFT→REVIEWING→APPROVED/REJECTED(→DRAFT)
  状态变更落 AuditLog；API：/api/version/audit/submit|approve|reject|history
- G2 推送渠道扩展：PushStrategy 加 DingTalkStrategy/WeComStrategy/EmailStrategy（webhook stub：
  校验渠道配置 → 写 PushLog SUCCESS；未配置 → 记录失败），注册进工厂
- G3 推送 API：/api/publish/push 支持 channel 参数 + versionId（审核通过后可推送）
- G4 报告生成完成自动 DRAFT（现状已是）；手动提交审核

### H 阶段 — 版本对比回溯
- H1 对比服务：compare_versions(base_id, target_id)
  - 指标 diff：alert/vuln 数值字段增量（total/high/close_rate/unfixed 等）+ 环比百分比
  - 章节文本 diff：difflib.SequenceMatcher 按章节比对，输出 新增/删除/修改 行摘要
  - API：/api/version/compare?baseId=&targetId=
- H2 前端：
  - 历史报告页：周期筛选已支持；版本行加「对比」按钮（勾选两版本）
  - 新增对比视图：指标变化表（左右值 + 增量）+ 章节 diff 高亮
  - 预览页加审核操作：提交审核/通过/驳回 + 推送渠道选择（local/dingtalk/wecom/email）

### 前端（配合后端）
- 版本对比页（H2）+ 审核/推送操作按钮 + 数据源类型展示（列表接口带 type label）

## 2. 文件级任务清单

```
sec_report_agent/
├── template/
│   ├── daily/daily_report.md.j2            [E1 新增]
│   ├── weekly/weekly_report.md.j2          [E1 新增]
│   ├── quarterly/quarterly_report.md.j2    [E1 新增]
│   └── yearly/yearly_report.md.j2          [E1 新增]
├── capability/
│   ├── adapter/
│   │   ├── excel_adapter.py                [F1 新增] openpyxl 解析 .xlsx
│   │   ├── intel_adapter.py                [F2 新增] IOC jsonl
│   │   ├── history_adapter.py              [F3 新增] MetricSnapshot 回读
│   │   └── factory.py                      [F] 注册 3 类
│   ├── render/
│   │   └── md_renderer.py                  [E2 修改] 按 cycle 选模板
│   └── message/push_strategy.py            [G2 修改] +3 渠道策略
├── app/services/
│   ├── audit_service.py                    [G1 新增] 审核状态机
│   ├── version_service.py                  [H1 新增] 版本对比
│   └── publish_service.py                  [G3 修改] channel 参数
├── api/routers/
│   ├── version.py                          [G1/H1 修改] audit/compare 端点
│   └── publish.py                          [G3 修改] channel
├── scripts/
│   ├── mock_data_gen.py                    [E3 修改] 全年参数化
│   └── init_datasources.py                 [F] 增加 3 类配置（幂等）
└── frontend/src/
    ├── views/Reports.vue                   [H2 修改] 对比入口
    ├── views/ReportPreview.vue             [H2 修改] 审核/推送操作
    └── views/VersionCompare.vue            [H2 新增] 对比视图
```

## 3. 关键设计决策

1. **审核状态机不建新表**：复用 ReportVersion.status（V1.0 已默认 DRAFT），状态变更落 AuditLog
2. **推送渠道 stub 化**：外网受限不真联网；策略校验渠道配置（config_json 有 webhook 地址）→
   写 PushLog SUCCESS（模拟响应）；未配置 → FAILED。生产换真实 HTTP 调用（策略内部实现）
3. **环比数据来源**：HistoryAdapter 从 MetricSnapshot 查上一周期同 cycle 快照 → trend.compare
4. **版本对比**：指标 diff 用数值字段递归对比；文本 diff 用 difflib 按章节行级比对（轻量零依赖）
5. **模板差异化**：日/周精简（无漏洞章节自动隐藏），季/年扩展（含环比对比段）；Jinja2 条件渲染
6. **mock 全年数据**：生成器按目标窗口动态生成（now 参数化），五周期窗口均有数据

## 4. 测试验收

- pytest 新增用例（预计 +25）：
  - 模板：五周期渲染断言（日/周无漏洞章节、季/年含环比段）
  - 适配器：Excel 解析 / 情报 IOC 解析 / History 环比
  - 审核：状态机流转（DRAFT→REVIEWING→APPROVED、驳回回 DRAFT）+ AuditLog 落库
  - 对比：指标 diff 数值正确、文本 diff 行级摘要
  - 推送：channel 校验、PushLog 落库
- 浏览器链路：五周期生成 → 版本对比页 → 审核流程 → 推送操作
- 全部完成后 git 提交（按功能点分批 commit）

## 5. 风险与对策

| 风险 | 对策 |
|---|---|
| 日报窗口（昨天）mock 数据覆盖 | 生成器 now 参数化，确保昨天~今天有数据 |
| 季/年报窗口数据量 | 全年生成器，季报 ~4500 条、年报 ~18000 条（可接受，SQLite/MySQL 均无压力） |
| 版本对比文本 diff 噪音 | 按章节行级比对，只输出摘要（增/删/改行数 + 关键行） |
| 审核状态机破坏 V1.0 兼容 | 默认 DRAFT 不变，仅新增流转端点 |
