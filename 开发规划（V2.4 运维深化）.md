# 多周期网安报告智能体 — V2.4 运维深化（第 7 轮）

编制：2026-08-07 ｜ 范围：内置自检告警（阈值 DB 热读）+ 日志结构化/轮转/脱敏（字段可配置）+ 静态交付物
依据：V2.3 交付后深化方向（告警规则/日志集中）；用户 2026-08-07 调整意见已并入

## 0. 现状基线

- 356 用例全绿，覆盖率 93%；V2.3 已交付 /health 就绪探针、/metrics Prometheus 指标、推送 real 化、审计页、路由权限
- 部署形态：Docker/systemd 单机，非 K8s → 告警走「应用自检 + 钉钉/企微推送」，不依赖企业监控栈

## 1. 范围

### A 内置自检告警器（阈值 DB 热读）

- 新表 `alert_rule`：rule_key(unique) / name / threshold / window_hours / enabled / updated_at / updated_by
  - 种子 3 条：task_fail_count（最近 1h FAILED ≥3）、llm_fallback_rate（最近 1h fallback 占比 >0.5）、push_fail_count（最近 1h 推送失败 ≥3）
- `infra/alert/alerter.py`：asyncio 后台循环（复用 simple_scheduler 模式），每轮从 DB 读规则（**热更新：改阈值/开关立即生效，无需重启**）
- 触发动作：AuditLog 记 ALERT_xxx + 复用 V2.3 推送策略发钉钉/企微（mock 模式记 PushLog）
- 防抖：同 rule_key 30 分钟不重复（内存记录，重启可接受丢失）
- API：GET/PUT /api/alert/rules（require_admin，查列表/改阈值开关）
- 前端：告警规则页（admin 菜单，表格 + 阈值/开关编辑）
- 指标：触发次数计 sec_report_alert_total

### B 日志结构化 + 轮转 + 脱敏（字段可配置）

- JSONFormatter：统一 {ts, level, logger, trace_id, event, msg, fields...}，文件输出 JSON（Loki/ELK 零转换采集）；控制台保持文本
- RotatingFileHandler：10MB × 5 备份（防磁盘打满）
- 脱敏过滤器：settings.sensitive_fields 可配置字段列表（默认 password/secret/token/authorization/api_key/cookie），递归打码 `***`；.env 可覆盖（SENSITIVE_FIELDS 逗号分隔）

### C 静态交付物（有监控栈客户抄作业）

- docs/observability/prometheus/alert-rules.yml：任务失败率/LLM 降级/推送失败/依赖挂 4 条规则
- docs/observability/promtail/config.yml：Loki 采集示例（JSON 日志直接吃）

## 2. 测试（test_v24，8 用例，用户精简版）

1. 告警触发-失败率：注入假指标 → check_and_alert 触发 → AuditLog + PushLog
2. 告警触发-LLM 降级：同上（不同 rule）
3. 防抖：连续两次 check → 只发一次
4. 日志 JSON 格式：一条日志 → JSON 字段齐全（ts/level/trace_id/msg）
5. 脱敏-默认字段：password 字段值被 ***
6. 脱敏-自定义字段：sensitive_fields 加 customer_id → 生效
7. 轮转：logger 配置 RotatingFileHandler（maxBytes/backupCount 断言 + doRollover 验证）
8. 静态文件语法：yaml.load(alert-rules.yml + promtail 示例)

## 3. 验收清单

- [ ] 告警器启动（lifespan 并列调度器），DB 改阈值 → 下一轮生效（无需重启）
- [ ] 触发后 AuditLog + 推送（mock PushLog）落库；同类型 30min 防抖
- [ ] 日志文件 JSON、控制台文本；10MB×5 轮转；敏感字段打码（默认 + 自定义）
- [ ] 前端告警规则页 admin 可改阈值/开关
- [ ] 全量回归 ≥356 全绿（覆盖率 ≥92%）
- [ ] 浏览器验收：规则页编辑 → 保存 → 生效

## 4. 风险

- 告警循环与测试隔离：check_and_alert() 独立可手动调用（不依赖循环）
- JSON 日志可能破坏现有日志断言：检查 tests 是否有 format 断言，有则兼容
- alert_rule 表迁移：复用 migrate.py 轻量建表
