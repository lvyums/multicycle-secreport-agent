# 多周期网安报告智能体（MultiCycle SecReport Agent）详细设计文档

# 1 文档概述

## 1\.1 文档目的

本文档为**网安多周期报告管理智能体**详细设计文档，用于明确系统整体架构、模块职责、业务流程、数据模型、技术方案、规则逻辑、落地约束与扩展能力。可直接作为研发开发、测试验收、项目立项、需求评审、交付归档的正式依据。

本智能体覆盖网络安全领域**日报、周报、月报、季报、年报**全周期自动化生成、研判、版本管理、人工编辑、分发归档、趋势分析能力，解决安全运营人员重复统计、人工写报告、研判不统一、归档混乱、数据不一致等痛点。

## 1\.2 产品定位

本系统属于**安全运营AI智能体（SOP自动化引擎）**，并非通用对话大模型。以「标准化安全数据底座\+确定性指标统计\+规则引擎\+RAG知识库\+LLM智能研判\+模板渲染引擎」为核心，实现**数据可溯源、数值无幻觉、研判可解释、报告可定制、版本可回溯**的企业级安全报告自动化体系。

## 1\.3 适用范围

适用于政企单位、企事业单位安全运营中心、等保运维、安全值守、常态化安全保障场景下的周期性安全报告自动产出与管理。可对接现有安全日志平台、态势感知、漏洞平台、资产平台、告警平台。

## 1\.4 前置依赖与能力复用

本智能体可完全复用现有「日志溯源卫士智能体」底座能力，无需重复建设：

- 多源日志解析、归一化、清洗能力

- 统一LLM调用工厂（LLMFactory）、Prompt管理能力

- 向量知识库RAG检索底座

- 安全事件时序存储、资产漏洞数据底座

# 2 需求分析

## 2\.1 业务需求

- 支持**日/周/月/季/年**五种时间粒度安全报告自动生成

- 实现安全告警、攻击行为、漏洞资产、处置工单、安全事件的自动统计与趋势分析

- 实现AI自动风险研判、问题总结、整改建议、下期工作计划生成

- 支持报告多版本管理、人工编辑、回溯对比、一键重生成

- 支持报告导出Word/PDF/Markdown、自动化推送、合规归档

- 适配不同单位自定义报告模板，兼容上级上报格式

## 2\.2 功能需求

1. **多源数据接入与标准化**：支持安全设备、告警、漏洞、资产、人工事件、外部情报、历史报告统一接入清洗

2. **定时调度引擎**：五种周期自动触发，支持手动自定义时间生成

3. **结构化指标聚合统计**：全量安全指标代码级精准计算，杜绝LLM幻觉数值

4. **规则\+LLM双路风险研判**：硬规则风险判定\+大模型语义分析、趋势解读、整改建议

5. **RAG知识库辅助分析**：历史报告、制度规范、处置案例、同比环比参考

6. **多周期模板渲染引擎**：日报轻量化、年报深度复盘的分级模板体系

7. **报告版本管理**：初稿、编辑版、终版全生命周期留存

8. **人工编辑与审核**：在线修改、批注、补充人工事件、修正研判结论

9. **分发与归档**：消息推送、邮件推送、文件归档、合规留存、检索查询

10. **系统配置管理**：调度时间、过滤规则、指标阈值、模板配置、推送对象配置

## 2\.3 非功能需求

- **准确性**：所有统计数值由代码计算，LLM仅做文字转述分析，严禁AI编造数据

- **可溯源**：所有报告数据可溯源至原始告警、漏洞、资产记录

- **稳定性**：支持数据补录重跑、数据缺失检测、异常提示

- **可扩展性**：新增指标、新增报告模板、新增数据源无需重构核心逻辑

- **合规性**：报告长期归档、版本留存、操作日志记录，满足等保审计要求

# 3 总体系统架构

## 3\.1 架构分层模型

系统采用六层分层架构，自上而下依次为：人机交互层 → 输出分发归档层 → 报告智能体核心引擎 → 时间粒度调度引擎 → 数据接入标准化层 → 多源数据源层。

完整架构链路：**原始数据采集 → 清洗归一化 → 时间窗口切片 → 指标结构化聚合 → RAG知识库检索 → 规则\+LLM研判 → 模板渲染生成报告 → 人工审核编辑 → 分发归档留存**

## 3\.2 各层整体职责

### 3\.2\.1 数据源层

汇聚所有安全运营原始数据，为报告生成提供完整素材，包含七大类数据源：

- 安全设备日志：防火墙、WAF、IDS/IPS、EDR、堡垒机、扫描设备、攻防演练日志

- 安全告警事件：入侵攻击、暴力破解、Web攻击、恶意代码、异常账号、权限变更

- 资产漏洞数据：资产清单、漏洞扫描结果、高危漏洞、修复进度、闭环状态

- 人工运营数据：人工处置记录、巡检记录、应急响应、安全变更、任务台账

- 外部情报数据：行业漏洞通告、重大威胁预警、公开安全事件

- 历史报告数据：历年日周月季年报，用于趋势对比、风格对齐、复盘分析

- 制度规范数据：等保要求、单位安全管理制度、考核指标、运维SOP

#### 3\.2\.1\.1 七大类数据源详细接入方案（协议/策略/模块/规则 可直接落地）

本智能体所有数据源接入统一遵循**适配器模式接入、原始落地存储、标准化清洗、结构统一输出**规范，所有外部异构数据通过独立适配器隔离，上层业务、指标计算、AI研判完全不感知底层接入差异，实现插拔式数据源扩展。

**通用接入强制约束（所有数据源生效）**：① 先落原始数据、后清洗加工，保证可回溯、可重算；② 统一归一化为标准数据结构体，字段、枚举、时间格式全局统一；③ 支持增量同步、定时补全、断连自愈、异常隔离；④ 接入失败不阻塞报告主流程，仅做数据缺失标记与告警。

##### 1\. 安全设备日志数据（防火墙/WAF/IDS/IPS/EDR/堡垒机/攻防设备）

**接入协议**：主流采用 Syslog UDP/TCP 实时流式采集，老旧设备兼容本地日志文件定时采集。

**同步策略**：秒级实时入库，每日凌晨自动分片补数，防止网络抖动、丢包导致的数据缺失；以时间窗口做增量切片，适配多周期统计。

**对应工程模块**：capability/data\_adapter/syslog\_adapter\.py

**适配清洗规则**：统一时区与时间格式、归一化设备类型、过滤心跳保活、测试噪音、重复风暴日志；自动识别攻击协议、攻击类型、资产对象，统一四级风险标签。

##### 2\. 安全告警事件数据（态势感知/告警平台/安全中枢）

**接入协议**：优先 REST API 增量拉取，支持 Kafka 消息订阅流式消费。

**同步策略**：5–10分钟定时增量轮询，以最后事件时间为游标增量拉取，避免全量拉取压力；支持断连续拉、重复去重。

**对应工程模块**：capability/data\_adapter/api\_adapter\.py

**适配清洗规则**：统一风险等级映射、告警状态归一化（待处置/已处置/误报）；基于「事件指纹\+资产\+时间窗口」全局去重，过滤重复告警、震荡告警。

##### 3\. 资产漏洞数据（资产平台/漏洞扫描平台/等保扫描数据）

**接入协议**：数据库直连（MySQL/PG）为主、平台API为辅。

**同步策略**：每日全量同步资产基线，小时级增量同步新增漏洞、修复状态变更、逾期未修复数据。

**对应工程模块**：capability/data\_adapter/db\_adapter\.py

**适配清洗规则**：资产存活状态归一化、漏洞CVSS分值自动映射高中低风险；自动计算修复时长、逾期状态、闭环率，统一漏洞类型字典。

##### 4\. 人工运营台账数据（巡检/值守/处置/应急/变更台账）

**接入协议**：Excel/表格离线导入 \+ 运维OA系统API在线同步双模式。

**同步策略**：支持手动批量导入、每日定时增量同步；支持补录历史台账后重跑对应周期报告。

**对应工程模块**：capability/data\_adapter/excel\_adapter\.py、业务台账API适配器

**适配清洗规则**：自动补全缺失时间、责任人、处置结果；统一人工事件分类，打上人工运维标签，区分机器告警与人工处置记录。

##### 5\. 外部安全情报数据（漏洞预警、行业威胁、重大安全事件）

**接入协议**：公开安全情报API、厂商威胁情报接口。

**同步策略**：每日定时更新，增量同步最新预警与重大威胁事件。

**对应用途**：用于报告宏观态势分析、外部风险预警、合规提示、趋势研判，不参与内部资产告警统计。

##### 6\. 历史周期报告数据（日/周/月/季/年历史报告）

**接入方式**：本地文档批量导入 \+ 向量库持久化入库。

**同步策略**：首次全量向量化入库，新报告生成后自动同步入库、增量更新知识库。

**对应工程模块**：capability/rag/doc\_loader\.py

**核心作用**：支撑同比环比趋势分析、历史问题复盘、报告风格对齐、往期整改落地跟踪。

##### 7\. 制度合规数据（等保规范/安全制度/SOP/考核标准）

**接入方式**：PDF/Word/MD离线文档导入，静态知识库维护。

**同步策略**：制度版本更新时手动更新知识库，长期静态复用。

**对应工程模块**：capability/rag/

**核心作用**：约束AI研判口径、规范整改建议、保证报告合规可解释、满足上级检查与等保审计依据。

#### 3\.2\.1\.2 数据接入完整流水线（标准化闭环）

**多源异构接入 → 独立适配器适配 → 原始数据落库留存 → 责任链清洗降噪/归一化/去重 → 统一标准结构体输出 → 指标统计聚合 → RAG知识匹配 → 双路风险研判 → 模板渲染出报告**

#### 3\.2\.1\.3 数据源接入核心收口规则

- 所有**外部动态数据**（设备、告警、漏洞、台账、情报）通过适配器接入，支持热插拔新增数据源；

- 所有**内部知识静态数据**（历史报告、制度规范）通过RAG文档库统一管理；

- 所有数据最终收敛为**同一套标准结构化字段**，上层业务完全解耦、无需适配多源差异。

### 3\.2\.2 数据接入与标准化层

统一解决多源数据格式杂乱、字段不统一、噪音过多问题，完全复用现有日志解析底座。

核心能力：

- 多协议接入：Syslog、文件采集、数据库同步、API拉取、Excel导入

- 字段标准化：统一输出「事件时间、资产IP、风险等级、事件类型、处置状态、处置人、设备来源」通用字段

- 数据清洗：去重、合并重复告警、过滤测试数据、过滤无效噪音

- 风险分级：自动划分高危/中危/低危/信息四级标签

- 时间戳绑定：所有数据强制绑定标准时间，用于周期切片统计

### 3\.2\.3 时间粒度调度引擎（核心差异化模块）

实现一套统计逻辑、适配五种报告周期，避免多套代码维护，支持自动化定时触发与手动自定义触发。

#### 3\.2\.3\.1 自动调度规则

- 日报：每日凌晨01:00，统计昨日00:00\-23:59全量数据

- 周报：每周一凌晨01:30，统计上周一至周日完整自然周数据

- 月报：每月1日凌晨02:00，统计上月完整自然月数据

- 季报：每季度首月1日凌晨02:30，统计上一季度自然周期数据

- 年报：每年1月1日凌晨03:00，统计上一自然年全年数据

#### 3\.2\.4\.2 调度辅助能力

- 手动重生成：支持自定义起止时间，任意周期手动生成报告

- 补录重跑：数据补录完成后，支持重新生成对应周期报告

- 数据依赖校验：生成前检测数据源完整性，缺失则报告标注数据异常

- 周期算子统一：同一套聚合公式，仅切换时间窗口，保证指标口径统一

### 3\.2\.4 报告智能体核心引擎（系统核心）

由五大子模块构成，实现「确定性统计\+规则风控\+AI智能分析\+模板渲染」全流程能力。

#### 3\.2\.4\.1 结构化指标聚合模块（数据保真底线）

所有统计数值**全部代码计算，不依赖大模型**，彻底杜绝幻觉问题，输出结构化JSON指标集，供后续研判与模板渲染使用。

统一指标库（全周期通用）：

- 告警事件指标：总事件数、高危/中危/低危/信息事件数、已处置/待处置数、闭环率

- 攻击行为指标：暴力破解、Web攻击、恶意代码、越权访问、异常登录等分类统计

- 资产漏洞指标：扫描资产总数、存活资产数、高危/中危漏洞数量、修复数量、修复率、未修复清单

- TOP统计：攻击源TOP10、被攻击资产TOP10、攻击类型TOP排行

- 对比指标：环比、同比数据，自动计算涨跌幅度

- 运维处置指标：人工处置事件数、应急响应次数、巡检完成率

#### 3\.2\.4\.2 规则\+LLM双路风险研判模块

采用「硬规则兜底、大模型赋能」的安全研判机制，兼顾准确性和专业性。

**1）硬规则引擎（确定性风险判定）**

- 高危告警超阈值、高危漏洞未修复超量，自动标记重点风险

- 出现勒索病毒、远程代码执行、核心资产越权等重大事件，自动标记重大安全风险

- 闭环率过低、长期未修复漏洞，自动输出整改预警

**2）LLM智能研判（语义分析与总结）**

输入内容：结构化指标JSON\+RAG检索上下文\+固定角色Prompt

输出内容：本期安全总体概览、风险特征分析、趋势变化、现存问题、针对性整改建议、下期工作重点。严格约束LLM：仅解读数据、不编造数据、不修改数值。

#### 3\.2\.4\.3 RAG知识库检索模块

构建专属安全报告知识库，让AI报告贴合企业实际、贴合历史风格、贴合合规要求。

知识库内容：

- 历史全周期报告：日/周/月/季/年报历史文档

- 企业安全制度、等保要求、安全考核标准

- 历史事件处置案例、整改记录、问题复盘文档

核心作用：同比趋势对比、复用成熟整改方案、对齐历史写作风格、保证报告专业度与一致性。

#### 3\.2\.4\.4 多周期模板渲染引擎

设计**差异化分级模板体系**，适配不同周期报告的详略要求，同时支持自定义模板适配上级上报格式。

|报告模块|日报|周报|月报|季报|年报|
|---|---|---|---|---|---|
|本期安全概览|精简版|标准版|标准版|详细版|深度综述|
|安全告警统计分析|✅|✅|✅|✅|✅|
|漏洞与资产风险分析|❌|✅|✅|✅|✅|
|重点事件处置复盘|精简|常规|详细|深度复盘|年度复盘总结|
|风险研判与问题总结|极简提示|常规分析|深度分析|专项研判|年度风险研判|
|整改建议与优化措施|简要|常规|细化落地|专项整改|年度优化规划|
|下期工作计划|❌|周度计划|月度计划|季度计划|年度工作计划|
|原始数据附录|可选|可选|可选|可选|可选|

模板技术：基于Jinja2模板引擎，支持占位符动态替换，可渲染Markdown、Word、PDF，支持用户上传自定义Word模板，适配各类上报格式。

#### 3\.2\.4\.5 报告版本管理模块

实现报告全生命周期版本管控，解决AI初稿不准、人工修改、版本混乱问题。

- 版本分类：AI自动初稿、人工编辑版、最终发布版

- 版本溯源：记录每版修改人、修改时间、修改内容、操作日志

- 版本对比：支持任意两个版本差异对比，快速查看修改点位

- 重生成覆盖：补录数据后可重新生成新版本，历史版本不丢失

### 3\.2\.5 输出分发归档层

- 多格式输出：Markdown、Word、PDF标准报告文件

- 智能分发：按周期配置接收人，日报推运维、月报推安全负责人、年报推管理层，支持企业微信/钉钉/邮件推送

- 合规归档：报告元数据\+文件双留存，支持按时间、类型、关键词检索，满足等保审计留存要求

- 在线预览：Web端直接查看报告内容、版本记录、生成日志

### 3\.2\.6 人机交互层

- 自动任务看板：展示各周期报告生成状态、待审核报告、异常报告

- 手动生成功能：自定义时间区间生成任意报告

- 在线编辑审核：富文本编辑、内容修改、批注、提交终审

- 历史报告检索：全周期报告查询、下载、预览、回溯

- 自动任务看板：展示各周期报告生成状态、待审核报告、异常报告

- 手动生成功能：自定义时间区间生成任意报告

- 在线编辑审核：富文本编辑、内容修改、批注、提交终审

- 历史报告检索：全周期报告查询、下载、预览、回溯

- 系统配置中心：调度规则、数据过滤、指标阈值、模板管理、推送配置、用户权限

#### 3\.2\.6\.1 前端可视化专项能力设计（数据源/文档/报告可视化选配）

为降低运维配置门槛、实现**零代码数据源接入、可视化知识库维护、可视化报告内容选配**，系统配套企业级前端可视化管理页面，支撑全部数据接入与报告定制能力可视化操作，无需修改后端代码、无需配置文件改动，全程页面可视化操作生效。

##### 1）多源数据源可视化接入管理页面

统一管理七大类数据源接入配置、在线状态、数据采集质量，支持插拔式可视化新增、编辑、启停数据源。

**页面核心功能**：

- **数据源总览看板**：可视化展示全部7类数据源接入状态（正常/异常/断连/空数据）、今日采集数据量、异常数据量、最近同步时间，支持状态色块区分、快速定位故障数据源。

- **可视化新增数据源**：针对不同适配器类型提供表单化配置：
Syslog设备：填写端口、协议类型、设备名称、归属分类，一键开启监听接入；

- API数据源：填写请求地址、密钥、请求周期、增量游标字段、请求头参数，可视化测试连通性；

- 数据库数据源：填写地址、账号、库表、查询SQL、同步频率，支持连接测试与数据预览；

- 文件台账数据源：支持Excel模板下载、在线批量导入、导入预览、数据校验。

- **数据源启停与热更新**：单个数据源支持独立启用/禁用，配置修改实时热更新，无需重启服务，不影响其他数据源采集任务。

- **接入质量可视化监控**：展示单数据源采集成功率、丢包率、脏数据占比、最近异常日志，支持查看原始采集样本、快速对账排查。

- **数据过滤白名单可视化配置**：页面可视化添加IP白名单、噪音日志过滤规则、测试数据拦截规则，实时生效。

##### 2）知识库文档可视化导入管理页面

针对历史报告、制度规范、运维案例等静态知识库，提供可视化文档管理、版本更新、向量化入库能力，运维人员可自主维护知识库。

**页面核心功能**：

- **文档批量上传导入**：支持PDF/Word/MD/TXT多格式文件拖拽上传，批量导入、单文件增补导入。

- **知识库分类管理**：可视化分类：历史周期报告、等保制度规范、安全运维SOP、风险处置案例、行业威胁情报，支持自定义分类。

- **文档版本管理**：同名文档支持版本覆盖、历史版本留存、版本对比，记录上传人、上传时间、更新日志。

- **向量化进度可视化**：展示文档解析、切片、向量化、入库进度，成功/失败状态可视化，失败文档展示异常原因。

- **知识库检索预览**：支持在线检索已入库知识库内容、预览切片片段、校验RAG召回效果。

##### 3）报告内容可视化选配与模板定制页面

支持用户**可视化勾选报告模块、自由选配展示内容、切换周期模板、自定义输出样式**，实现不同场景、不同上级上报格式的零代码适配。

**页面核心功能**：

- **报告周期可视化选择**：支持日报/周报/月报/季报/年报一键切换，页面实时展示对应周期的默认模板结构。

- **报告模块可视化勾选**：以可视化组件形式展示所有报告章节，支持自由勾选开启/关闭：安全概览、告警统计、资产漏洞分析、重点事件复盘、风险研判、整改建议、下期计划、数据附录等，按需组装报告内容。

- **模板可视化管理**：内置全套分级模板，支持上传自定义Word模板、预览模板样式、设置默认上报模板，支持模板绑定指定报告周期。

- **指标可视化选配**：支持自定义报告展示指标、TOP统计数量、同比环比展示开关、风险等级展示阈值，可视化配置即刻生效。

- **输出格式可视化选择**：生成报告前可可视化选择输出格式：Markdown/Word/PDF，支持批量导出、打包下载。

##### 4）数据接入与任务可视化日志页面

- 全量数据采集任务日志、重跑任务记录、文档导入记录、报告生成日志统一可视化展示；

- 支持按数据源、时间、任务状态、异常类型筛选检索；

- 支持查看单任务完整链路TraceID、原始数据样本、异常堆栈，可视化问题定位。

##### 前端可视化设计核心约束

- 所有前端配置**热更新生效**，无需后端改代码、无需重启服务；

- 所有接入、导入、配置操作**留存操作日志、操作人员、操作时间**，满足合规审计；

- 前端仅做配置与可视化展示，核心数据校验、清洗、权限校验全部后置，保证系统安全稳定；

- 完全适配系统插拔式架构，新增数据源、新增模板自动适配前端可视化面板，无需迭代改造页面。

#### 3\.2\.6\.2 前端整体组件架构设计（分层解耦、可复用、可扩展）

前端采用**分层组件化架构**，遵循「底层公共组件→业务通用组件→页面业务组件→页面入口」四层结构，完全解耦UI与业务逻辑，统一复用、统一维护、适配系统插拔式扩展能力，支撑数据源配置、文档导入、报告可视化选配全场景。

##### 1\. 前端四层组件分层体系

- **第一层：基础公共组件层（全局通用）**：封装系统基础原子组件，全局统一样式与交互，无业务逻辑。包含：表单输入、弹窗、按钮、表格、标签页、进度条、状态标签、文件上传、下拉选择、消息提示、异常空状态组件。所有页面统一复用，禁止页面自定义原生样式与基础控件。

- **第二层：业务通用组件层（领域复用）**：针对本系统网安报告业务封装通用业务组件，可跨页面复用。包含：数据源状态卡片、数据采集日志列表、文档导入进度组件、报告模块选配组件、周期选择器、模板预览组件、任务状态监控组件。

- **第三层：页面业务组件层（页面私有）**：单页面专属拼装组件，由公共组件\+业务组件组合而成，承载页面细分业务逻辑。包含：数据源配置表单、多类型适配器配置面板、知识库分类管理面板、报告章节可视化配置面板、任务重跑操作栏。

- **第四层：页面入口层（路由入口）**：页面路由入口，仅负责组件拼装、权限判断、页面初始化，不编写复杂业务逻辑与接口请求逻辑。包含：数据源管理页、知识库文档管理页、报告定制配置页、任务日志监控页、报告编辑预览页。

##### 2\. 核心页面组件拆分明细

**（1）数据源可视化管理页组件拆分**

- 数据总览统计卡片组件：展示数据源总数、在线数、异常数、今日采集总量

- 数据源列表组件：展示所有适配器数据源、状态、同步时间、操作入口

- 多类型动态配置表单组件：根据适配器类型（Syslog/API/数据库/文件）动态渲染对应配置项

- 连通性测试组件：实时测试数据源连通状态、展示测试结果

- 数据质量监控组件：展示采集成功率、脏数据占比、异常日志预览

- 过滤规则配置组件：可视化配置白名单、噪音过滤规则

**（2）知识库文档导入管理页组件拆分**

- 文档拖拽上传组件：支持批量上传、格式校验、大小校验

- 知识库分类树组件：自定义分类、层级管理

- 文档列表与版本管理组件：展示文档信息、版本记录、更新日志

- 向量化进度可视化组件：实时展示解析、切片、入库进度

- 文档预览与检索组件：在线预览、关键词检索、切片片段查看

**（3）报告可视化选配与模板管理页组件拆分**

- 报告周期切换组件：日/周/月/季/年一键切换，联动更新模板结构

- 报告模块可视化勾选组件：树形可视化勾选所有报告章节，支持批量启用/禁用

- 指标参数配置组件：自定义TOP数量、阈值、同比环比展示开关

- 模板上传与预览组件：自定义模板上传、在线预览、周期绑定配置

- 报告输出配置组件：选择MD/Word/PDF输出格式、批量导出配置

**（4）任务日志与监控页组件拆分**

- 任务状态统计组件：待执行/执行中/成功/异常任务统计

- 日志筛选检索组件：多维度条件筛选、TraceID精准检索

- 任务详情弹窗组件：展示任务链路、原始数据、异常堆栈

##### 3\. 前端架构核心约束

- 逻辑与视图分离：接口请求、数据处理、状态管理统一抽离，页面仅负责渲染与交互

- 配置驱动视图：所有报告模块、指标、数据源参数由后端配置下发，前端动态渲染

- 权限分层控制：页面级、按钮级、操作级权限统一管控，无权限操作隐藏禁用

- 权限分层控制：页面级、按钮级、操作级权限统一管控，无权限操作隐藏禁用

- 全局状态统一：采用全局状态管理任务状态、数据源状态、配置信息，避免数据冗余

##### 4\. 页面加载全流程细化规范（加载优先级、异步策略、兜底容错）

为保证前端页面打开速度快、状态反馈清晰、极端场景不白屏、不报错，系统统一制定**页面初始化加载流程、资源优先级、异步拆分、缓存复用、异常兜底**标准，所有可视化页面统一遵循该加载规范。

###### 4\.1 页面加载分层优先级策略

页面初始化采用「先骨架、后静态、再动态、最后重载」的分层加载机制，杜绝一次性大批量请求阻塞页面渲染。

- **一级优先（页面瞬时渲染）**：页面骨架布局、固定菜单、权限按钮、静态文案、基础组件，本地静态渲染，无需接口请求，实现页面秒开，无空白等待。

- **二级优先（核心业务数据）**：登录权限信息、全局系统配置、数据源状态概览、报告任务统计数据，页面挂载后优先请求，支撑页面核心视图展示。

- **三级异步加载（非核心列表数据）**：详细列表、日志记录、文档列表、历史配置数据，核心视图渲染完成后异步延迟加载，不阻塞首屏展示。

- **懒加载（重型资源）**：模板预览渲染、文档内容预览、大数据量图表、超长日志详情，采用视口懒加载，进入可视区域后再请求与渲染。

###### 4\.2 标准页面初始化加载流程

1. **页面挂载初始化**：加载全局样式、基础组件、路由权限校验，未登录/权限不足直接拦截跳转，终止后续加载逻辑。

2. **骨架屏展示**：接口请求期间统一展示对应模块骨架屏，杜绝空白页、闪烁抖动，提升加载感知体验。

3. **全局基础数据加载**：请求用户信息、角色权限、系统全局配置、字典枚举，缓存至全局状态，整站复用。

4. **首屏核心数据异步加载**：根据当前页面场景，请求核心统计数据、状态数据、配置数据，渲染首屏核心模块。

5. **次级数据延迟加载**：首屏渲染完成后，通过定时器/钩子函数异步加载列表、日志、明细等非核心数据。

6. **重型资源懒加载**：页面滚动、模块展开时，动态加载预览、图表、详情等重型资源。

7. **加载状态销毁**：所有接口请求完成/异常结束后，统一关闭骨架屏、loading状态，展示最终页面内容。

###### 4\.3 各类页面专属加载策略

**（1）数据源管理页加载策略**

- 优先加载数据源总览统计卡片，快速展示接入状态、数据总量、异常数量；

- 异步加载数据源列表、同步日志、采集质量数据；

- 新增/编辑弹窗配置表单懒加载，点击弹窗后再初始化对应适配器表单组件；

- 连通性测试、数据预览等高频操作单独独立请求，不影响页面整体加载状态。

**（2）知识库文档管理页加载策略**

- 优先加载知识库分类树结构，支撑用户快速切换分类；

- 异步加载当前分类文档列表、版本信息；

- 文档预览、向量化详情采用点击懒加载，避免批量文档渲染卡顿；

- 上传组件预加载基础能力，大文件解析进度实时异步回调展示。

**（3）报告可视化选配页面加载策略**

- 优先加载周期选择器、默认模板基础配置；

- 异步加载报告模块勾选配置、指标阈值配置、历史模板列表；

- 模板预览窗口懒加载，点击预览后再渲染Word/PDF预览视图；

- 模块勾选变更采用局部刷新，不整页重载。

**（4）任务日志监控页面加载策略**

- 优先加载任务状态统计概览；

- 分页异步加载日志列表，默认只加载第一页，避免大数据量卡顿；

- 任务详情、链路日志弹窗懒加载，点击详情后请求完整链路数据。

###### 4\.4 前端数据缓存与重载策略

- **全局静态缓存**：系统字典、权限枚举、固定模板配置本地长效缓存，页面刷新优先复用，后台更新后自动过期刷新。

- **页面临时缓存**：单页面列表数据、配置数据页面生命周期内缓存，避免频繁重复请求。

- **配置热更新重载**：用户提交配置后，局部刷新对应模块数据，无需整页重载，保证操作流畅。

- **手动刷新能力**：所有页面提供「刷新」按钮，支持用户主动重载最新服务端数据。

###### 4\.5 加载容错与兜底规范（杜绝白屏、报错崩溃）

- **接口超时兜底**：接口超时自动终止请求、关闭loading，展示「数据加载超时，请稍后重试」友好提示，页面不空白、不崩溃。

- **空数据兜底**：列表、统计数据为空时展示自定义空状态组件，提示暂无数据，无空白留白。

- **接口异常兜底**：接口400/500异常时，保留页面已有视图，展示异常提示，支持一键重试，不销毁页面整体结构。

- **网络异常兜底**：断网、网络波动自动捕获，展示网络异常提示，监听网络恢复后可自动重试加载。

- **批量请求容错**：页面多接口并行加载时，单接口失败不影响其他模块正常渲染，失败模块独立兜底展示。

###### 4\.6 加载交互体验统一规范

- 全局统一loading样式、骨架屏样式，所有页面交互视觉一致；

- 所有异步操作必带加载状态，防止用户重复点击、重复提交；

- 加载完成/操作成功统一轻量提示，异常精准描述问题原因与解决建议；

- 长时任务（文档解析、报告生成）展示实时进度条与进度文本，动态刷新进度。

#### 3\.2\.6\.3 前后端接口调用统一规范（联调标准）

为保证前后端联调统一、接口稳定、可维护、可追溯，系统定义**统一接口请求方式、请求头、入参格式、响应格式、异常处理、接口命名规范**，所有前端页面必须严格遵循。

##### 1\. 基础请求规范

- 请求协议：统一 HTTPS/HTTP 内部协议

- 请求方式：查询类用 GET、新增/配置/操作类用 POST、编辑更新用 PUT、删除用 DELETE

- 超时配置：统一接口超时 30s，文件上传超时 120s

- 身份鉴权：所有接口统一携带 Token 请求头，无 Token 直接拦截跳转登录

- 链路追踪：所有请求自动携带 Trace\-ID，贯穿前后端全链路，用于问题排查

##### 2\. 统一请求参数规范

**通用请求头固定参数**：Token、Trace\-ID、Timestamp、Client\-Type

**通用分页入参（所有列表接口统一）**：page、pageSize、keyword、startTime、endTime、status

**业务配置入参规范**：所有配置类提交参数统一封装为 JSON 对象，禁止零散参数传递，便于后端配置热更新存储。

##### 3\. 统一接口响应格式（全局固定）

所有后端接口返回结构统一，前端统一解析、统一异常处理，杜绝多格式混乱：

- code：状态码（200成功、500服务异常、401权限过期、403无权限、400参数错误）

- message：返回提示信息（成功/错误描述）

- data：业务返回数据（对象/数组/空值统一兼容）

- traceId：链路追踪ID，用于日志溯源

- timestamp：接口响应时间

##### 4\. 接口分类命名规范

统一接口路由前缀，按业务模块拆分，清晰易维护：

- **/api/data/adapter**：数据源接入配置、启停、测试、规则配置接口

- **/api/knowledge**：知识库文档上传、导入、版本管理、检索接口

- **/api/report/config**：报告周期、模块、指标、模板定制配置接口

- **/api/report/task**：报告任务触发、重跑、状态查询、日志查询接口

- **/api/report/render**：报告渲染、导出、预览接口

- **/api/system**：系统权限、用户、全局配置接口

##### 5\. 特殊场景接口调用规范

**（1）文件上传接口规范**

- 采用 FormData 格式提交，支持批量文件上传

- 后端返回统一进度回调、成功文件信息、失败原因

- 前端实时展示上传进度、向量化解析进度，失败精准提示

**（2）长时任务接口规范（报告生成、数据重跑）**

- 长耗时任务采用「异步触发 \+ 轮询查询状态」模式

- 前端触发任务后，定时轮询任务状态，实时更新进度与结果

- 任务成功/失败统一弹窗提示、日志留存

**（3）配置热更新接口规范**

- 所有页面配置提交后，后端实时热更新生效，无需重启服务

- 前端提交成功后，自动刷新本地配置缓存，保证页面展示与服务端一致

##### 6\. 全局异常拦截与提示规范

- 401：自动清空登录态，跳转登录页

- 403：统一提示「无操作权限」，禁用对应操作按钮

- 400：展示后端返回参数错误信息，定位表单问题

- 500：统一服务异常提示，展示TraceID，支持一键复制排查

- 网络超时/断网：统一兜底提示，禁止空白报错

##### 7\. 前后端联调核心约束

- 所有接口字段、枚举、状态码前后端统一对齐，禁止前端私自写死业务状态

- 所有页面数据优先后端驱动，前端仅做展示与交互，不做业务计算、不做数据判定

- 所有操作必须有加载状态、成功提示、异常兜底，交互完整闭环

- 新增数据源、新增模板、新增报告模块，接口自动适配，无需前端改代码

# 4 核心业务流程设计

## 4\.1 标准报告生成全流程

1. **周期调度触发**：定时任务或手动触发报告生成任务

2. **数据切片拉取**：根据时间窗口拉取本期所有标准化安全数据

3. **数据校验与清洗**：检测数据完整性、过滤噪音、去重聚合

4. **结构化指标计算**：代码统计全量指标，输出结构化JSON

5. **RAG知识库检索**：召回历史报告、制度规范、同类处置案例

6. **双路风险研判**：规则引擎判定硬性风险 \+ LLM生成智能分析文本

7. **模板渲染生成初稿**：指标\+分析文本填充模板，生成标准报告初稿

8. **版本入库**：保存AI初稿版本，记录生成日志

9. **消息推送提醒**：通知安全人员审核报告

10. **人工编辑审核**：修正内容、补充人工事件、调整研判结论

11. **终版归档分发**：确认终版，自动归档、按需推送、留存备查

## 4\.2 数据补录重跑流程

1. 人工补录历史告警、漏洞、处置数据

2. 手动触发对应周期报告重生成

3. 系统重新聚合最新全量数据

4. 生成新版本报告，保留旧版本不覆盖

5. 支持版本对比，确认数据修正效果

# 5 核心约束与风险规避设计（关键落地规范）

## 5\.1 数据准确性约束（最高优先级）

- 所有数字、统计比例、排行数据**禁止LLM计算**，全部由后端代码精准统计

- LLM仅负责文字解读、趋势分析、问题总结、建议输出

- 报告所有数据支持溯源，点击指标可查看对应原始数据清单

## 5\.2 数据降噪约束

- 自动过滤设备保活、测试告警、重复风暴告警，避免报告失真

- 支持自定义白名单过滤规则，适配各单位特殊环境

## 5\.3 数据缺失容错约束

- 生成报告前自动校验数据源在线状态与数据完整性

- 数据缺失时，报告头部明确标注「本期部分数据源未采集，数据存在缺失」，不隐瞒、不造假

## 5\.4 人机协同约束

- 智能体仅输出**初稿**，不可直接对外发布

- 所有报告必须经过人工审核、修正、确认后才可归档分发

- 高危风险研判结论必须人工复核

# 6 技术选型方案

- **后端框架**：FastAPI（高性能接口、任务管理）

- **定时调度**：APScheduler（支持Cron表达式、动态任务管理）

- **业务数据库**：MySQL（存储报告元数据、版本、配置、操作日志）

- **时序数据存储**：TimescaleDB/InfluxDB（存储安全事件时序数据）

- **向量知识库**：Milvus（RAG检索、历史报告向量化）

- **模板渲染**：Jinja2、python\-docx、weasyprint

- **大模型调用**：自研LLMFactory统一调用底座

- **消息推送**：钉钉/企业微信机器人、SMTP邮件服务

# 7 系统扩展能力设计

- **报告智能问答**：支持对历史所有报告进行对话检索、数据查询、复盘问答

- **指标异常预警**：指标突增突降自动识别，报告附带风险预警通知

- **上级格式适配**：可定制各类上级单位、监管单位上报模板，一键导出上报文件

- **多维度对比分析**：支持周期对比、年度对比、专项风险对比分析报告

- **权限分级管理**：支持管理员、审核员、查看员多角色权限控制

# 8 总结

本网安多周期报告管理智能体，通过**数据标准化底座\+统一时间调度引擎\+确定性指标统计\+规则\+LLM双路研判\+RAG知识库赋能\+可定制模板与版本管理**的整套架构，彻底解决传统安全报告人工成本高、数据不准、研判不统一、归档混乱、无法溯源的问题。系统最大化复用现有智能体底座能力，开发成本低、落地快、稳定性强，完全满足企业常态化安全运营与合规上报需求。

# 9 企业级落地实现方案（解耦架构 \+ 设计模式 \+ 工程方法论）

本章为系统**工程落地设计规范**，不编写具体业务代码，仅论述**模块拆分原则、解耦方案、设计模式选型、分层调用机制、工程规范、扩展机制**，完全符合企业级后端研发标准，可直接指导开发人员落地编码、保证代码低耦合、高复用、易维护、可扩展。

## 9\.1 整体工程设计思想

### 9\.1\.1 核心设计原则

本系统落地严格遵循六大企业级开发原则，从根源规避代码臃肿、耦合严重、难以迭代的问题：

- **单一职责原则**：每一个类、每一个服务、每一个方法仅负责一项能力，杜绝万能类、万能工具类

- **开闭原则**：对扩展开放、对修改关闭，新增报告类型、新增指标、新增模板无需修改核心源码

- **依赖倒置原则**：上层业务依赖抽象接口，不依赖具体实现，便于后续替换存储、替换LLM、替换调度引擎

- **接口隔离原则**：细分业务接口，避免大而全的臃肿接口，按需暴露能力

- **迪米特法则**：模块之间通过统一中间层调用，禁止跨模块直接调用内部方法

- **数据与逻辑分离**：统计数据纯代码计算、业务逻辑分层处理、AI能力独立封装，完全解耦

### 9\.1\.2 全局解耦方案

系统采用**四层彻底解耦架构**：基础底座层、通用能力层、业务服务层、接口应用层，层级单向依赖，禁止逆向依赖、禁止跨层调用。

- 基础底座层：数据库、缓存、向量库、调度、文件存储、消息推送（通用底层能力，无业务逻辑）

- 通用能力层：日志解析、数据清洗、RAG检索、LLM调用、模板渲染、指标计算（通用工具能力）

- 业务服务层：报告调度、报告生成、风险研判、版本管理、审核归档（核心业务逻辑）

- 接口应用层：对外API、参数校验、权限控制、请求分发（仅做接入与转发，无核心逻辑）

## 9\.2 核心模块拆分与解耦实现方法

按照业务边界对系统进行模块化拆分，每个模块独立封装、独立迭代、独立单元测试，模块通信统一通过**服务接口**实现，无硬编码依赖。

### 9\.2\.1 定时调度模块（独立模块）

**模块职责**：统一管理日/周/月/季/年周期任务、手动触发任务、补录重跑任务，不参与任何数据计算与报告生成逻辑。

**解耦方式**：调度模块仅负责触发任务，通过事件总线/接口回调调用报告生成服务，自身不包含任何业务逻辑。

**采用设计模式**：

- **工厂模式**：根据任务类型（日报/周报/月报）动态生成对应周期任务实例，统一任务创建入口

- **观察者模式**：任务执行完成、任务异常、数据缺失等事件统一发布，由日志服务、消息推送服务、版本服务订阅消费

- **策略模式**：不同周期采用不同时间切片策略，统一调度入口、差异化时间逻辑

**落地规范**：所有Cron配置、时间窗口参数、任务开关统一写入配置中心，支持动态热更新，无需重启服务。

### 9\.2\.2 数据接入与标准化模块（独立模块）

**模块职责**：负责多源数据接入、清洗、去重、归一化、风险分级，输出标准化结构化数据，不参与统计、不参与报告渲染。

**解耦方式**：定义统一数据标准化接口，所有数据源适配该接口，新增设备、新增数据类型无需修改清洗核心逻辑。

**采用设计模式**：

- **适配器模式**：针对Syslog、API、数据库、Excel等不同数据源，编写独立适配器，统一输出标准结构体

- **责任链模式**：数据校验→去重→降噪→分级→时间校准，逐层链式处理，每层逻辑独立可单独开启/关闭

- **单例模式**：全局清洗规则、过滤白名单单例加载，全局统一规则

### 9\.2\.3 指标聚合统计模块（独立核心模块）

**模块职责**：纯结构化数据计算，输出所有安全统计指标，**无任何AI逻辑、无任何文本生成逻辑**，保证数据绝对准确。

**解耦方式**：指标计算服务完全独立，仅依赖标准化数据，不依赖LLM、不依赖模板、不依赖推送服务，可单独单元测试、单独校验数据准确性。

**采用设计模式**：

- **策略模式**：同比、环比、TOP排行、风险统计采用独立计算策略，统一指标输出入口

- **模板方法模式**：五种周期指标复用统一计算模板，仅时间窗口变量差异化

- **缓存代理模式**：周期指标计算结果缓存，重跑任务优先复用有效缓存，提升性能

### 9\.2\.4 风险研判模块（规则\+LLM双路，独立模块）

**模块职责**：接收结构化指标\+RAG上下文，完成硬规则风险判定与LLM智能分析，输出研判文本与风险标签。

**解耦方式**：规则引擎与LLM引擎完全拆分，可独立启用、独立关闭，研判结果统一封装结构体输出，与报告模板解耦。

**采用设计模式**：

- **策略模式**：高/中/低风险、重大安全事件采用不同研判策略，差异化输出结论

- **装饰器模式**：在基础LLM分析能力上，叠加制度约束、历史风格、合规要求等装饰能力

- **工厂模式**：根据报告周期（日/周/月/季/年）生成不同深度的Prompt模板实例

### 9\.2\.5 RAG知识库检索模块（独立底座模块）

**模块职责**：负责文档向量化、相似度检索、历史数据召回、制度案例匹配，仅输出上下文数据，不参与业务研判。

**解耦方式**：标准化检索接口，上层业务仅调用检索方法，不感知向量库存储、分词、向量化细节。

**采用设计模式**：

- **外观模式**：封装向量入库、分词、检索、过滤复杂逻辑，对外提供极简统一接口

- **缓存策略模式**：热点历史报告、高频制度文档开启缓存检索，提升响应速度

### 9\.2\.6 模板渲染模块（独立展示模块）

**模块职责**：接收结构化指标\+研判文本，根据周期模板渲染生成Markdown/Word/PDF，仅负责视图组装，无任何数据计算逻辑。

**解耦方式**：数据层与视图层彻底分离，渲染服务只消费数据，不生产数据，新增模板、修改模板不影响核心业务。

**采用设计模式**：

- **工厂模式**：根据报告周期自动匹配对应内置模板，支持自定义模板实例动态加载

- **策略模式**：不同输出格式（MD/Word/PDF）采用独立渲染策略

- **原型模式**：模板文件预加载为原型实例，每次渲染克隆实例填充数据，避免重复IO加载

### 9\.2\.7 版本管理模块（独立治理模块）

**模块职责**：报告版本生成、版本存储、版本对比、版本回溯、操作日志记录。

**解耦方式**：所有报告生成、编辑、重写动作统一触发版本服务记录，业务模块无需关注版本逻辑。

**采用设计模式**：

- **备忘录模式**：保存报告每一个版本快照，支持随时回溯恢复

- **责任链模式**：版本新增→日志记录→文件归档→索引更新链式执行

### 9\.2\.8 分发归档模块（独立输出模块）

**模块职责**：文件存储、多渠道推送、合规归档、检索索引维护。

**解耦方式**：推送、存储、归档能力完全独立，可单独开关、单独配置，不影响报告生成主流程。

**采用设计模式**：

- **策略模式**：企业微信、钉钉、邮件、本地归档采用不同分发策略

- **异步观察者模式**：报告终审完成后异步触发分发与归档，不阻塞主流程

## 9\.3 统一工程分层调用规范（企业级标准）

严格执行**分层单向调用机制**，杜绝乱调用、循环依赖、跨层耦合，所有业务执行必须遵循以下层级顺序：

**接口层 → 参数校验 → 业务服务层 → 通用能力层 → 底座层**

具体约束规范：

- 接口层禁止出现任何计算逻辑、AI逻辑、文件操作逻辑，仅做请求接收、权限校验、参数校验

- 业务服务层禁止直接操作数据库、禁止直接调用LLM接口，必须通过通用能力层封装调用

- 通用能力层禁止新增业务规则，仅提供通用工具能力

- 底座层仅提供基础读写、调度、存储能力，无任何业务判断

## 9\.4 核心业务流程的代码解耦执行逻辑

完整报告生成流程采用**组装式、插件式**执行，每一步独立可插拔，支持单独禁用、单独迭代：

1. 调度服务触发任务（无业务逻辑）

2. 数据服务完成切片、清洗、标准化（纯数据处理）

3. 指标服务完成结构化统计（纯计算逻辑）

4. RAG服务完成上下文召回（纯检索逻辑）

5. 研判服务完成规则校验\+AI分析（纯文本研判）

6. 模板服务完成视图渲染（纯组装逻辑）

7. 版本服务保存初稿快照（纯版本治理）

8. 推送服务异步通知审核（纯消息分发）

任意模块可独立优化、独立替换，不影响整条链路其他能力。

## 9\.5 全局异常与日志工程方案

企业级落地必须统一异常体系，杜绝裸异常、杂乱日志，保证线上可排查、可追溯。

- **自定义业务异常体系**：定义数据异常、调度异常、LLM异常、模板异常、权限异常统一异常枚举

- **全局异常拦截器**：统一捕获异常、统一返回格式、统一日志打印，业务层无需try\-catch

- **链路追踪规范**：所有报告生成任务绑定唯一TraceID，贯穿数据查询、计算、研判、渲染全流程

- **分级日志规范**：INFO记录流程、WARN记录风险、ERROR记录故障、DEBUG记录调试细节

## 9\.6 配置化与热更新实现方案

所有可变参数**全部配置化**，杜绝代码硬编码，实现业务无停机更新：

- 调度时间、任务开关、重跑策略配置化

- 风险阈值、过滤白名单、数据降噪规则配置化

- Prompt模板、报告模块显隐、输出格式配置化

- 推送人员、推送渠道、归档保留时长配置化

所有配置支持**动态热更新**，修改配置即刻生效，无需重启服务。

## 9\.7 扩展性落地设计（适配长期迭代）

基于接口\+设计模式的架构，天然支持横向扩展，无需重构核心代码：

- **新增报告周期**：新增时间策略\+新增模板即可，无需修改计算、研判、渲染核心逻辑

- **新增统计指标**：新增指标计算策略，自动适配所有周期报告

- **新增AI模型**：替换LLM底层实现类，上层业务完全无感知

- **新增数据源**：新增数据适配器，即可接入新设备数据

- **新增上报格式**：上传自定义模板即可适配，无需开发改造

## 9\.8 单元测试与质量保障方案

企业级落地必须具备可测试性，解耦架构可实现模块独立单元测试：

- 指标计算模块：批量构造测试数据，校验统计数值准确性，杜绝AI幻觉与计算错误

- 规则研判模块：覆盖高、中、低、重大风险场景，测试规则命中准确性

- 模板渲染模块：测试不同周期、不同模板渲染效果一致性

- 调度模块：测试定时任务、重跑任务、异常重试机制有效性

所有核心能力自动化测试覆盖率保证80%以上，保障迭代不破坏原有业务逻辑。

## 9\.9 最终工程落地优势总结

通过本章节**模块化解耦\+标准化分层\+合理设计模式选型\+企业级工程规范**的落地方法，系统实现以下核心工程优势：

- 高内聚低耦合：模块职责清晰，无交叉逻辑，迭代维护成本极低

- 可扩展可插拔：新增功能无需改核心代码，符合长期迭代需求

- 高稳定高可靠：数据逻辑与AI逻辑隔离，杜绝数值错误、业务异常

- 可测试可追溯：全链路日志、统一异常、独立单元测试，线上问题快速定位

- 企业级标准化：完全适配政企项目研发、交付、运维规范，可直接用于生产环境落地

> （注：部分内容可能由 AI 生成）
