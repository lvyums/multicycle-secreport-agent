# 多周期网安报告智能体 开发规划（落地执行版）

> 本规划基于文件夹内三份文档（《多周期网安报告智能体.md》《详细设计文档》《迭代开发规划.md》）编制，
> 是**唯一执行依据**：明确技术选型定版、底座复用清单、基础设施轻量化方案、目录落地结构、数据模型、
> 首轮任务清单（具体到文件）、接口清单、测试验收标准与风险约束。
> 编制时间：2026-08-06，基于本机实测环境。

---

# 1 规划结论总览（关键决策）

## 1.1 技术栈定版（本机实测可用，零新增联网依赖）

| 层 | 选型 | 版本(实测) | 说明 |
|---|---|---|---|
| Web框架 | FastAPI | 0.136.1 | 文档指定 |
| ORM | SQLAlchemy 2.0 + PyMySQL（同步） | 2.0.49 / 1.1.0 | **MySQL 8（Docker compose 一键启停）**；URL 可切 SQLite 兜底 |
| 缓存 | redis 客户端 | 6.4.0 | **Redis 7（Docker）**；内存 TTL 缓存兜底 |
| 向量库 | ChromaDB（复用底座） | 1.5.9 | 文档指定 Milvus → 开发期沿用底座 ChromaDB（Milvus 需 etcd+minio 三件套较重），接口抽象可换 |
| LLM | openai SDK（复用 LLMFactory） | 2.45.0 | 配置指向 raytoken.com.cn deepseek |
| 校验/配置 | pydantic 2.13 / pydantic-settings 2.14 | 已装 | 全局配置中心 |
| 前端 | Vue3 + Element Plus + Vite + TS | node v24 / npm 11 | 复用旧项目前端架构模式 |
| 定时调度 | **自研零依赖 asyncio 调度器** | — | APScheduler 未装且外网受限，见 §1.3 |

**结论：Python/前端依赖全部本机已具备，无需 pip/npm 联网安装；仅 MySQL/Redis 镜像需 docker pull 一次（网络受限时有 SQLite/内存缓存一键降级兜底，见 §1.3）。**

## 1.2 底座复用清单（来自日志溯源卫士智能体 /mnt/e/Code/code.python/agent/log_trace_guard_agent/）

详细设计文档 §1.4 明确"可完全复用现有日志溯源卫士智能体底座能力"，具体映射：

| 新项目位置 | 来源文件 | 复用方式 |
|---|---|---|
| infra/vector/vector_store.py | core/ai_base/vector_store.py | 原样拷贝（ChromaDB + 多级降级，sentence-transformers 缺失时自动降级） |
| capability/rag/rag_facade.py | core/ai_base/rag_factory.py | 原样拷贝 + 检索缓存装饰 |
| capability/rag/doc_loader.py | core/ai_base/kb_ingest.py | 原样拷贝 |
| capability/judge/llm_factory.py | core/ai_base/llm_factory.py | 拷贝 + 适配本项目的 settings/日志 |
| capability/judge/prompt_manager.py | core/ai_base/prompt_manager.py | 拷贝 + 新增周期 Prompt 版本管理 |
| common/logger/ | common/logger.py | 原样拷贝 |
| common/utils/ | common/{time_util,json_util,file_util,str_util,ip_util}.py | 原样拷贝 |
| common/result_util | common/result_util.py | 保留（内部服务返回），API 层统一 ApiResponse |
| capability/judge/rule_engine | core/rule_engine/ | 按报告研判场景精简重写（阈值规则化） |

> 拷贝原则：只搬"通用技术能力"，不搬任何旧业务模块（log_parse/script_gen 等）与旧前端页面。

## 1.3 基础设施方案（Docker 一键启停 + 抽象兜底）

开发期直接使用 Docker 运行 MySQL/Redis（一键启动/关闭，轻量无负担）；其余重型组件按抽象隔离、留生产替换口。

| 组件 | 开发期实现 | 启动方式 | 生产 |
|---|---|---|---|
| MySQL | MySQL 8.0（Docker） | `docker compose up -d mysql` | 同构 |
| Redis | Redis 7（Docker） | `docker compose up -d redis` | 同构 |
| Milvus | ChromaDB（复用底座） | 本地 | 换 VectorStore 实现类 |
| TimescaleDB/InfluxDB | MySQL 时序表（std_event 带时间索引） | 随 MySQL | 换时序库仓储 |
| APScheduler | 自研 asyncio 调度器（cron 解析 + 轮询触发） | — | 换 APScheduler 实现 |
| weasyprint PDF | V1.1 只出 MD+Word；PDF 接口占位 | — | 装 weasyprint 后补 pdf_render |
| 钉钉/企微/邮件推送 | 本地推送日志 stub | — | 加渠道策略实现 |

配套 docker-compose.yml（工程根目录，`docker compose up -d` 一键启动 / `docker compose down` 一键关闭）：

```yaml
services:
  mysql:
    image: mysql:8.0
    container_name: sec-report-mysql
    environment:
      MYSQL_ROOT_PASSWORD: sec_report_dev
      MYSQL_DATABASE: sec_report
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    ports: ["3306:3306"]
    volumes: [mysql-data:/var/lib/mysql]
  redis:
    image: redis:7
    container_name: sec-report-redis
    ports: ["6379:6379"]
volumes:
  mysql-data:
```

**降级兜底（关键）**：若镜像拉取/启动失败（网络受限），.env 一行切换即可回退，业务零改动：
- `DATABASE_URL=mysql+pymysql://sec_report:sec_report_dev@127.0.0.1:3306/sec_report` ⇄ `sqlite:///./sec_report.db`
- `CACHE_BACKEND=redis` ⇄ `CACHE_BACKEND=memory`

> 设计原则：**文档的四层架构（api→service→capability→infra）保证底座可替换**，上层业务只依赖抽象接口、不感知存储/缓存实现；
> 单元测试默认用 SQLite 内存库（快、隔离、CI 不依赖 Docker），联调/演示/定时任务用 MySQL/Redis。

## 1.4 迭代映射与本次交付范围

| 文档版本 | 内容 | 本规划轮次 |
|---|---|---|
| V1.0 | 全域框架底座（一次性建好） | **第 1 轮（本次）：阶段 A** |
| V1.1 | 核心业务最小闭环（月报） | **第 1 轮（本次）：阶段 B/C/D** |
| V1.2 | 全周期 + 全数据源 + 审核推送 | 第 2 轮 |
| V1.3 | 前端可视化零代码运营 | 第 3 轮 |
| V2.0 | 生产级加固 | 第 4 轮 |

**本次交付目标（第 1 轮）**：V1.0 框架底座 + V1.1 月报最小业务闭环 —— 可运行、可演示、可验收：
mock 数据源 → 清洗 → 指标 → RAG → 规则+LLM 研判 → 月报渲染(MD/Word) → 版本留存 → 前端任务看板 + 历史报告列表。

---

# 2 工程目录结构（最终落地版）

严格对齐主文档 §10 目录规范（api/service/capability/infra 四层），仅做文件级落地细化：

```plain text
multicycle-secreport-agent/
├── docker-compose.yml                 # MySQL8 + Redis7 一键启动/关闭（§1.3）
├── sec_report_agent/                  # 主工程（与三份设计文档同级）
│   ├── main.py                        # FastAPI 入口：中间件/路由注册/启动调度器
│   ├── requirements.txt               # 依赖版本锁定（全部已装）
│   ├── .env.example                   # 配置模板（LLM/DB/调度/阈值）
│   │
│   ├── api/                           # 接口应用层（无业务逻辑）
│   │   ├── __init__.py
│   │   ├── response.py                # 统一响应 ApiResponse{code,message,data,traceId,timestamp}
│   │   ├── deps.py                    # 全局依赖：TraceID 注入、参数校验、权限占位
│   │   └── v1/
│   │       ├── __init__.py
│   │       ├── report_api.py          # /api/report/task 生成/重跑/状态/日志
│   │       ├── schedule_api.py        # /api/report/config 调度配置/手动触发
│   │       ├── version_api.py         # /api/report/version 版本列表/对比/回溯
│   │       └── publish_api.py         # /api/report/render 导出/预览；/api/report/audit 审核
│   │
│   ├── service/                       # 业务服务层（编排、状态机，无技术实现）
│   │   ├── __init__.py
│   │   ├── report_service.py          # ★ 报告主流程编排（8 步插件式链路 + 状态机）
│   │   ├── schedule_service.py        # 任务生命周期：注册/触发/重跑/补录
│   │   ├── version_service.py         # 版本治理：快照/对比/回溯
│   │   ├── audit_service.py           # 审核流转：提交/终审/驳回
│   │   └── publish_service.py         # 分发归档编排（V1.1 本地落盘 + 推送 stub）
│   │
│   ├── capability/                    # 通用能力层（原子能力，无业务规则）
│   │   ├── __init__.py
│   │   ├── data_adapter/
│   │   │   ├── __init__.py
│   │   │   ├── adapter_base.py        # ★ 数据源适配器抽象（拉取/增量游标/字段映射/重试）
│   │   │   ├── syslog_adapter.py      # 本地日志文件模拟 Syslog 流
│   │   │   ├── api_adapter.py         # mock JSON 事件模拟告警平台 REST（增量游标语义）
│   │   │   ├── db_adapter.py          # SQLite 资产漏洞台账模拟资产库
│   │   │   ├── factory.py             # 适配器工厂（按 DataSourceType 注册）
│   │   │   └── data_chain.py          # ★ 清洗责任链：校验→去重降噪→归一化→分级→时间切片
│   │   ├── metric/
│   │   │   ├── __init__.py
│   │   │   ├── metric_base.py         # ★ 周期指标计算模板方法（五周期共用）
│   │   │   ├── alert_metric.py        # 告警指标策略
│   │   │   ├── vuln_metric.py         # 漏洞资产指标策略
│   │   │   ├── top_metric.py          # TOP 排行策略
│   │   │   ├── trend_metric.py        # 环比/同比策略
│   │   │   ├── factory.py             # 指标策略工厂
│   │   │   └── metric_cache.py        # 指标缓存代理（重跑复用）
│   │   ├── judge/
│   │   │   ├── __init__.py
│   │   │   ├── rule_engine.py         # ★ 硬规则引擎（阈值规则，确定性判定）
│   │   │   ├── llm_judge.py           # LLM 语义研判（复用 LLMFactory，无 LLM 时降级）
│   │   │   ├── judge_strategy.py      # 风险分级策略（高/中/低/重大）
│   │   │   ├── prompt_factory.py      # 周期 Prompt 工厂 + Prompt 版本管理
│   │   │   └── llm_factory.py         # （自底座拷贝）统一 LLM 调用入口
│   │   │   └── prompt_manager.py      # （自底座拷贝）Prompt 管理
│   │   ├── rag/
│   │   │   ├── __init__.py
│   │   │   ├── rag_facade.py          # 检索外观（复用底座，加缓存）
│   │   │   ├── doc_loader.py          # 文档入库向量化（复用底座）
│   │   │   └── rag_cache.py           # 检索缓存策略
│   │   ├── render/
│   │   │   ├── __init__.py
│   │   │   ├── render_base.py         # ★ 渲染抽象接口
│   │   │   ├── template_factory.py    # 周期模板工厂（内置 + 自定义目录）
│   │   │   ├── md_render.py           # Jinja2 → Markdown
│   │   │   ├── docx_render.py         # python-docx → Word
│   │   │   └── pdf_render.py          # V1.2+ 占位（返回未支持提示）
│   │   └── message/
│   │       ├── __init__.py
│   │       └── push_strategy.py       # 推送策略（V1.1 本地日志 stub，预留渠道）
│   │
│   ├── infra/                         # 基础底座层（纯技术能力，零业务）
│   │   ├── __init__.py
│   │   ├── db/
│   │   │   ├── __init__.py
│   │   │   ├── session.py             # engine/session（MySQL8 Docker 默认，URL 可切 SQLite 兜底）
│   │   │   ├── base.py                # Declarative Base
│   │   │   └── repositories.py        # 实体仓储（任务/版本/事件/指标/日志）
│   │   ├── vector/
│   │   │   ├── __init__.py
│   │   │   └── vector_store.py        # 复用底座：ChromaDB + 多级降级
│   │   ├── cache/
│   │   │   ├── __init__.py
│   │   │   └── cache.py               # Redis 默认 + 内存 TTL 兜底（CACHE_BACKEND 切换）
│   │   ├── schedule/
│   │   │   ├── __init__.py
│   │   │   ├── scheduler_base.py      # ★ 调度抽象（add_job(cron, func)）
│   │   │   └── simple_scheduler.py    # 零依赖 asyncio 调度器（cron 解析 + 轮询触发）
│   │   ├── storage/
│   │   │   ├── __init__.py
│   │   │   └── file_store.py          # 报告文件存储（规范命名 + 归档目录）
│   │   └── trace/
│   │       ├── __init__.py
│   │       └── trace.py               # TraceID 生成/上下文/日志注入
│   │
│   ├── common/                        # 全局公共组件
│   │   ├── __init__.py
│   │   ├── constant/                  # 全局枚举常量（周期/状态/风险/数据源类型）
│   │   ├── exception/                 # 自定义异常体系 + 全局异常拦截器
│   │   ├── logger/                    # 分级日志封装（复用底座）
│   │   ├── validator/                 # 通用参数校验器
│   │   └── utils/                     # 通用工具（复用底座 time/json/file/str/ip）
│   │
│   ├── config/                        # 全局配置中心（100% 配置化，热更新）
│   │   ├── __init__.py
│   │   ├── settings.py                # pydantic-settings：.env 加载（LLM/DB/路径）
│   │   ├── app_config.py              # 应用级配置
│   │   ├── schedule_config.py         # 五周期 Cron 规则 + 任务开关
│   │   ├── risk_config.py             # 风险阈值（高危告警数/未修复量/闭环率）
│   │   ├── filter_config.py           # 过滤白名单/降噪规则
│   │   ├── llm_config.py              # LLM 参数（温度/超时/重试/降级开关）
│   │   └── publish_config.py          # 推送对象/归档保留时长
│   │
│   ├── model/                         # 全局数据模型（收敛统一）
│   │   ├── __init__.py
│   │   ├── enum/                      # ReportCycle/TaskStatus/ReportStatus/DataSourceType/RiskLevel
│   │   ├── entity/                    # ORM 实体（见 §3）
│   │   ├── struct/                    # StdEvent/MetricSet/JudgeResult/RenderData/EmptyReport
│   │   └── dto/                       # API 入参出参 Pydantic
│   │
│   ├── template/                      # 报告模板资源
│   │   ├── monthly/                   # V1.1 先落地月报模板（MD + docx 结构）
│   │   ├── daily/ weekly/ quarterly/ yearly/   # V1.2 补齐
│   │   └── custom/                    # 自定义模板目录（V1.2+）
│   │
│   ├── frontend/                      # 前端工程（Vue3 + Element Plus + Vite + TS）
│   │   ├── package.json
│   │   ├── vite.config.ts
│   │   ├── index.html
│   │   └── src/
│   │       ├── main.ts
│   │       ├── App.vue                # 全局布局（侧边栏 + 顶栏 + 路由出口）
│   │       ├── api/                   # Axios 统一封装（响应拦截/TraceID/401/403/500 兜底）
│   │       ├── router/                # vue-router hash 模式 + 路由骨架
│   │       ├── store/                 # 全局状态（用户/系统配置/任务状态）
│   │       ├── components/            # 基础原子组件（状态标签/空状态/骨架屏/进度条）
│   │       ├── business/              # 业务通用组件（任务状态卡片/周期选择器/风险标签）
│   │       └── pages/                 # 页面入口
│   │           ├── Login.vue          # 登录骨架（V1.0）
│   │           ├── Dashboard.vue      # 报告任务看板（V1.1）
│   │           ├── Reports.vue        # 历史报告列表/预览/下载/手动生成（V1.1）
│   │           └── ...                # V1.3 数据源管理/知识库/选配/日志页
│   │
│   ├── scripts/dev/
│   │   ├── mock_data_gen.py           # ★ 生成模拟安全数据（月窗口告警/漏洞/攻击事件）
│   │   └── start_infra.sh             # docker compose up/down/ps 快捷脚本
│   │
│   └── tests/                         # 单元测试（模块独立，见 §7）
│       ├── conftest.py
│       ├── test_metric/  test_judge/  test_render/
│       ├── test_data_adapter/  test_schedule/
│       └── test_service/              # 报告主流程状态机测试
│
└── （三份设计文档 + 本规划）
```

---

# 3 数据模型设计（实体/枚举/状态机）

## 3.1 枚举（model/enum）

| 枚举 | 取值 |
|---|---|
| ReportCycle | DAILY / WEEKLY / MONTHLY / QUARTERLY / YEARLY |
| TaskStatus | PENDING / RUNNING / SUCCESS / EMPTY / FAILED / PARTIAL |
| ReportStatus | DRAFT(初稿) / REVIEWING(审核中) / APPROVED(终审) / ARCHIVED(归档) / FAILED |
| DataSourceType | SYSLOG / API / DB / EXCEL / INTEL / HISTORY / POLICY |
| RiskLevel | HIGH / MEDIUM / LOW / INFO |
| VersionType | AI_DRAFT / HUMAN_EDIT / FINAL |

## 3.2 ORM 实体（infra/db，MySQL 落地；单元测试用 SQLite 内存库）

| 实体 | 关键字段 | 用途 |
|---|---|---|
| DataSourceConfig | name, type, status(启停), config_json, filter_rules_json, sync_strategy | 数据源配置（热更新） |
| RawEvent | source_type, raw_content, receive_time, trace_id, status | 原始数据落地（先落原始库） |
| StdEvent | event_time, asset_ip, risk_level, event_type, status, device_source, source_ref, dedup_key | 标准化事件（指标计算输入） |
| AssetVuln | asset_ip, vuln_name, cvss, risk_level, status, fix_deadline, fix_time | 资产漏洞台账 |
| ReportTask | cycle, window_start, window_end, status, trigger_type, trace_id, error_msg | 报告任务 |
| ReportVersion | task_id, version_no, version_type, content_md, file_path, operator, created_at | 版本快照（备忘录模式） |
| MetricSnapshot | task_id, metrics_json | 指标快照（溯源/对比/防幻觉） |
| AuditLog | operator, action, target, detail, ip, trace_id | 操作审计 |
| PushLog | version_id, channel, status, detail | 推送记录 |

## 3.3 状态机（service 层强制约束）

```
任务状态机:  PENDING → RUNNING → SUCCESS / EMPTY / FAILED / PARTIAL
             FAILED/PARTIAL --手动重跑/补录--> PENDING   （禁止失败直接跳成功）
报告状态机:  DRAFT → REVIEWING → APPROVED → ARCHIVED
             FAILED --重生成--> DRAFT
每次状态变更落 AuditLog（操作者/时间/触发来源/TraceID）
```

---

# 4 首轮任务清单（V1.0 框架 + V1.1 月报闭环）

> 严格遵循迭代规划"框架只建一次、后续纯增量"铁律。每阶段结束有独立验收点（§7）。

## 阶段 A：V1.0 框架底座（对应文档 V1.0）

**A0 工程初始化**
- [ ] 初始化 git 仓库（git init + .gitignore：.env/__pycache__/reports/vector_data/*.db 等）
- [ ] 建目录骨架（§2 全量目录）+ requirements.txt 版本锁定
- [ ] .env.example + config/settings.py（pydantic-settings 加载）
- [ ] docker-compose.yml（MySQL8 + Redis7）+ 启动验证（`docker compose up -d` 后连接测试，含降级切换验证）

**A1 全局通用能力封装**
- [ ] common/exception：自定义异常体系（数据/调度/LLM/模板/权限/存储异常）+ 全局异常拦截器
- [ ] api/response.py：统一响应 ApiResponse（code/message/data/traceId/timestamp）+ 状态码常量
- [ ] infra/trace/trace.py：TraceID 生成 + contextvar 上下文 + 中间件注入
- [ ] common/logger：分级日志（INFO/WARN/ERROR/DEBUG），日志自动携带 TraceID
- [ ] common/validator：通用参数校验器（非空/范围/枚举/长度）
- [ ] common/constant：全局枚举常量收敛

**A2 底座层落地**
- [ ] infra/db：session.py（MySQL8 默认 + SQLite 兜底 URL 切换，SQLAlchemy 同步 + PyMySQL）+ base.py + repositories.py（9 实体仓储）
- [ ] infra/vector：拷贝 vector_store.py（ChromaDB + 多级降级）
- [ ] infra/cache：Redis 默认 + 内存 TTL 兜底（get/set/delete，支持过期，按 CACHE_BACKEND 切换）
- [ ] infra/schedule：scheduler_base.py 抽象 + simple_scheduler.py（cron 解析 + asyncio 轮询）
- [ ] infra/storage：file_store.py（报告目录规范：reports/{cycle}/{YYYY}/{MM}/）
- [ ] main.py：FastAPI 实例 + 中间件（TraceID/CORS/异常拦截）+ 路由注册框架 + 启动钩子（建表/启动调度器）

**A3 设计模式基类抽象（capability 层）**
- [ ] data_adapter/adapter_base.py：适配器抽象（fetch_window/增量游标/字段映射/重试/状态回调）
- [ ] data_adapter/data_chain.py：责任链基类（节点可配置启停）
- [ ] metric/metric_base.py：指标计算模板方法（五周期时间窗口参数化）
- [ ] judge/rule_engine.py：规则引擎基类（阈值规则注册）
- [ ] render/render_base.py：渲染抽象（render(data, template) → 文件路径）
- [ ] message/push_strategy.py：推送策略基类

**A4 前端框架（V1.0 骨架）**
- [ ] vite 工程初始化 + 依赖（vue/element-plus/vue-router/axios）+ hash 路由
- [ ] api/ 统一请求封装（拦截器：TraceID 头/401 跳登录/403/500 兜底/超时 30s）
- [ ] App.vue 全局布局（侧边栏菜单 + 顶栏）+ 登录页骨架 + 首页骨架
- [ ] components/ 基础原子组件：状态标签、空状态、骨架屏、进度条、消息提示

**✅ 阶段 A 验收**：`uvicorn main:app` 启动无报错；/api/health 返回统一响应带 traceId；前端 dev 启动出布局骨架；目录与基类齐全，无任何业务功能。

## 阶段 B：V1.1 数据接入 + 清洗 + 指标（纯数据链路）

**B1 mock 数据源**
- [ ] scripts/dev/mock_data_gen.py：生成一个月窗口模拟数据（告警事件 500+ 条、漏洞台账 50+ 条、攻击事件，含重复/噪音/脏数据样本）
- [ ] syslog_adapter：读取模拟日志文件，按行解析为 RawEvent
- [ ] api_adapter：读取 mock JSON 事件（模拟分页 + 增量游标语义）
- [ ] db_adapter：从 SQLite 台账表读资产漏洞
- [ ] factory.py：按 DataSourceType 注册三类适配器
- [ ] config/filter_config.py：白名单/噪音规则（心跳保活/测试日志过滤）

**B2 清洗责任链（data_chain.py）**
- [ ] 节点1 格式校验（字段完整性/时间格式/空值拦截）
- [ ] 节点2 去重降噪（事件指纹 dedup_key + 时间窗口去重 + 噪音过滤）
- [ ] 节点3 字段归一化（时间格式统一/风险枚举统一/事件类型字典）
- [ ] 节点4 风险分级（按规则映射 HIGH/MEDIUM/LOW/INFO）
- [ ] 节点5 时间切片对齐（按报告周期窗口裁剪）
- [ ] 清洗输出 StdEvent 落库 + 异常数据归档（RawEvent.status=ERROR 保留原样）

**B3 指标聚合（metric/）**
- [ ] alert_metric：总事件数/各级别数/已处置/待处置/闭环率
- [ ] vuln_metric：资产总数/存活数/高危中危漏洞/修复数/修复率/未修复清单
- [ ] top_metric：攻击源 TOP10/被攻击资产 TOP10/攻击类型 TOP
- [ ] trend_metric：环比（上月）/同比（去年同月）——V1.1 数据不足时返回 None 占位
- [ ] metric_base 模板方法 + factory 注册 + metric_cache 缓存代理
- [ ] 输出 MetricSnapshot 落库（metrics_json）

**✅ 阶段 B 验收**：脚本生成数据 → 三类适配器拉取 → 清洗 → 指标输出，数值与手工核对一致（pytest 断言精确值）；脏数据被正确过滤并归档。

## 阶段 C：V1.1 研判 + 渲染 + 版本（业务闭环）

**C1 双路风险研判（judge/）**
- [ ] rule_engine：阈值规则（高危告警>N / 高危漏洞未修复>M / 闭环率<K → 重点风险；勒索/RCE/越权事件 → 重大风险）
- [ ] prompt_factory：月报 Prompt（输入=指标JSON+RAG上下文，硬约束"只解读数据、不编造数值"）
- [ ] llm_judge：调用 LLMFactory 生成 概览/风险特征/趋势/问题/整改建议/下期重点；LLM 失败/超时 → 模板降级文本 + 标注"AI分析异常"
- [ ] judge_strategy：风险分级输出 JudgeResult 结构体
- [ ] rag_facade + doc_loader：历史报告/制度文档入库（内置 2-3 篇示例制度文档），月报生成时召回 top_k

**C2 模板渲染（render/）**
- [ ] template/monthly/report.md.j2：月报模板（概览/告警统计/漏洞资产/重点事件/风险研判/整改建议/下期计划）
- [ ] md_render：Jinja2 填充（只消费结构化指标 + 研判文本，禁止 LLM 直接输出数值）
- [ ] docx_render：python-docx 生成 Word 版月报
- [ ] template_factory：按周期匹配模板 + 预留自定义目录

**C3 版本管理（service/version_service.py）**
- [ ] 初稿快照：生成 AI_DRAFT 版本（备忘录模式：content_md + MetricSnapshot + 文件）
- [ ] 版本列表/对比/回溯接口（V1.1 先做列表 + 内容查看，对比/回溯 V1.2 完善）

**✅ 阶段 C 验收**：手动触发 → 全链路生成月报 MD + Word 文件；指标数值与 LLM 文本分离（渲染文件里数值均来自 MetricSnapshot）；无 LLM 时仍能出报告（降级路径）。

## 阶段 D：V1.1 API + 调度 + 前端页面

**D1 接口层（api/v1/）**
- [ ] report_api：POST /api/report/task/generate（手动指定周期+窗口）、POST /rerun、GET /list、GET /{id}、GET /{id}/log
- [ ] schedule_api：GET/POST /api/report/config（五周期 Cron + 开关）、GET /api/report/schedule/next
- [ ] version_api：GET /api/report/version/list?task_id=、GET /{version_id}/content
- [ ] publish_api：GET /api/report/render/export?version_id=&format=md|docx、GET /preview
- [ ] 长时任务规范：generate 异步触发 → 前端轮询任务状态

**D2 调度（service/schedule_service.py + infra/schedule）**
- [ ] 五周期 Cron 注册（月报：每月1日 02:00，其余周期 V1.2 启用开关）
- [ ] 手动触发 / 重跑（幂等：同窗口同任务不重复生成）
- [ ] 任务状态机全流转 + AuditLog 埋点

**D3 前端页面（V1.1）**
- [ ] Dashboard.vue 报告任务看板：任务状态统计卡片 + 任务列表（周期/窗口/状态/耗时/TraceID）+ 手动生成弹窗
- [ ] Reports.vue 历史报告列表：周期筛选/时间区间检索 + 预览（MD 渲染）+ 下载（md/docx）+ 手动重跑入口
- [ ] 统一加载规范：骨架屏/空状态/超时兜底/批量请求容错（对齐详细设计 §3.2.6.2）

**✅ 阶段 D 验收**：前端看板 → 手动生成月报 → 轮询状态 → 列表出现新报告 → 预览/下载成功；调度器到点自动触发月报任务；全链路 TraceID 可查。

---

# 5 API 接口清单（v1 定稿）

> 统一前缀按详细设计 §3.2.6.3；响应统一 ApiResponse；请求头统一 Token/Trace-ID/Timestamp/Client-Type。

| 模块 | 方法与路径 | 说明 | 版本 |
|---|---|---|---|
| 任务 | POST /api/report/task/generate | 手动生成（cycle + startTime/endTime） | V1.1 |
| 任务 | POST /api/report/task/rerun | 失败/补录后重跑（幂等） | V1.1 |
| 任务 | GET /api/report/task/list | 任务分页（keyword/status/时间区间） | V1.1 |
| 任务 | GET /api/report/task/{id} | 任务详情（含 TraceID/指标摘要） | V1.1 |
| 任务 | GET /api/report/task/{id}/log | 任务链路日志 | V1.1 |
| 配置 | GET/POST /api/report/config | 周期 Cron + 开关（热更新） | V1.1 |
| 配置 | GET /api/report/config/next | 下次触发时间预览 | V1.1 |
| 版本 | GET /api/report/version/list | 版本列表（task_id 过滤） | V1.1 |
| 版本 | GET /api/report/version/{id}/content | 版本内容（MD） | V1.1 |
| 版本 | GET /api/report/version/compare | 版本对比 | V1.2 |
| 渲染 | GET /api/report/render/export | 导出 md/docx（pdf V1.2+） | V1.1 |
| 渲染 | GET /api/report/render/preview | 在线预览 | V1.1 |
| 审核 | POST /api/report/audit/submit | 提交终审 | V1.2 |
| 审核 | POST /api/report/audit/approve | 终审通过 → 归档 | V1.2 |
| 数据源 | /api/data/adapter/* | 接入配置/启停/测试/规则 | V1.3 |
| 知识库 | /api/knowledge/* | 文档上传/检索 | V1.2/V1.3 |
| 系统 | /api/system/* | 登录/用户/配置（骨架） | V1.0 |

---

# 6 后续版本规划（第 2~4 轮，仅列任务不展开）

## 第 2 轮：V1.2 全周期 + 全数据源 + 审核推送
- 日/周/季/年四周期调度 + 差异化模板（对齐详细设计模板分级表）+ 同比环比补全
- Excel 台账适配（CSV 先行，xlsx 需装 openpyxl）、外部情报适配、历史报告入库
- 数据补录/任务重跑/版本对比回溯/历史版本留存
- 报告在线编辑（内容修改 + 人工事件补充 + 版本记录）、审核终审状态流转
- 报告推送（邮件/钉钉/企微策略实现）+ 合规归档 + 业务日志留存

## 第 3 轮：V1.3 前端可视化零代码运营
- 数据源可视化管理页（总览卡片/动态配置表单/连通性测试/质量监控/过滤规则）
- 知识库文档管理页（拖拽上传/分类树/版本管理/向量化进度/检索预览）
- 报告可视化选配页（周期切换/模块勾选/指标配置/模板上传预览/输出格式）
- 任务日志监控页（统计/多维筛选/TraceID 溯源/异常详情）

## 第 4 轮：V2.0 生产级加固
- 精细化 RBAC 权限 + 操作审计 + 配置变更留存 + 文件合规归档
- 指标计算缓存/大数据渲染优化/任务容错自愈/超时重试
- 高阶 AI：报告智能问答/指标异常预警/专项报告
- 单元测试覆盖率 ≥80% 全量达成 + 配置中心热更新完善 + 监控埋点

---

# 7 测试策略与验收标准

## 7.1 分层测试（对齐文档"核心覆盖率 ≥80%"）

| 层 | 用例重点 | 目录 |
|---|---|---|
| 数据适配 | mock 数据 → 适配器解析字段正确；脏数据/空数据/断连分支 | tests/test_data_adapter/ |
| 清洗链 | 去重（重复风暴）、降噪（心跳/测试日志）、归一化、分级、切片；节点可单独启停 | tests/test_data_adapter/ |
| 指标计算 | **构造固定输入 → 断言精确数值**（防幻觉核心）；环比/TOP/闭环率边界 | tests/test_metric/ |
| 规则引擎 | 高/中/低/重大风险命中场景 + 阈值边界 | tests/test_judge/ |
| LLM 研判 | mock LLMFactory 返回固定文本 → 断言结构体；LLM 失败 → 降级路径（复用旧项目 LLM 链路测试手法） | tests/test_judge/ |
| 渲染 | 模板填充断言（数值必须来自指标快照）；docx 生成成功 | tests/test_render/ |
| 调度/服务 | 状态机流转（含非法跳转拦截）、幂等重跑、全链路编排（mock 各 capability） | tests/test_schedule/ + test_service/ |

## 7.2 联调验证（对齐用户"轻量 mock 验证"偏好）
- mock 数据生成器 + MySQL/Redis（Docker 一键启动），数据链路真实落库
- uvicorn 起服务 → 前端浏览器链路测试（复用旧项目 browser-testing-workflow：导航/弹窗/Toast/兜底）
- LLM 链路：优先 mock LLMFactory（不真实调用 raytoken），确认无误后再开真实调用一次验证
- 调度验证：把月报 Cron 临时改为"每 30 秒"观察自动触发

## 7.3 每轮交付验收（迭代规划 §4 对齐）
- 第 1 轮：工程启动无报错；月报从数据到文件全链路可跑；前端看板/列表可用；pytest 全绿；git 提交清晰
- 每轮收尾：`git commit` 自然检查点（功能完成即提交，中文+英文描述，沿用旧项目规范）

---

# 8 风险与约束

| # | 风险 | 应对 |
|---|---|---|
| 1 | MySQL/Redis 镜像拉取可能受网络限制 | docker compose 一键方案 + 配置降级兜底（SQLite/内存缓存，业务零改动，见 §1.3）；启动失败先验证 `docker pull mysql:8.0` |
| 2 | APScheduler 未安装，外网受限 | 自研零依赖 asyncio 调度器（§1.3），接口抽象，后续可换 |
| 3 | sentence-transformers 未安装（BGE 嵌入不可用） | 复用底座 vector_store 多级降级（ChromaDB 默认嵌入/hash），开发期 RAG 可用；生产装 ST |
| 4 | weasyprint 未安装（PDF 不可用） | V1.1 只交付 MD+Word，PDF 接口占位返回提示；V1.2 评估安装 |
| 5 | 真实数据源（Syslog/告警平台/资产库）不可用 | mock 数据源适配器 + mock_data_gen.py 生成真实形态数据（含脏数据样本） |
| 6 | Excel xlsx 依赖 openpyxl 未装 | V1.1 不涉及；V1.2 CSV 先行，xlsx 需用户确认联网安装 |
| 7 | LLM 调用失败/超时 | llm_judge 全链路降级：规则引擎结论 + 模板文本 + "AI分析异常"标注，报告照常生成 |
| 8 | 外网受限导致无法装新 Python 包 | Python 依赖全部本地已装（§1.1），第 1 轮零 pip 安装；仅 Docker 镜像需 pull（有兜底） |
| 9 | 旧项目底座文件带旧业务耦合 | 只拷贝通用能力文件（LLM/RAG/工具），逐文件审查去耦，不拷贝业务模块 |

---

# 9 与旧项目的关系（边界声明）

- 旧项目（日志溯源卫士智能体）已归档收尾，本次为**全新独立工程**，独立 git 仓库
- 复用方式 = 拷贝通用能力 + 按新架构适配，不建立跨项目运行时依赖
- 旧项目沉淀的 skill（log-trace-guard-dev）中通用工程手法（工厂注册/LLM 截断恢复/责任链/测试手法）继续沿用；
  本智能体开发到 V1.2 后沉淀独立 skill（multicycle-secreport-dev）
