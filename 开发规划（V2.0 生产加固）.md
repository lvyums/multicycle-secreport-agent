# 多周期网安报告智能体 — V2.0 生产加固（第 4 轮）

编制：2026-08-06  |  范围：RBAC / 性能 / 容错 / 高阶 AI / 覆盖率 ≥80%

## 0. 现状基线

- pytest 54 用例全绿；覆盖率 **47%**（3494 行，缺口 ~1160 行到 80%）
- 0% 大块：app/scheduler(24) / llm_factory(115) / llm_judge(50) / prompt_builder(59) / prompt_manager(31) / doc_loader(107) / rag_facade(29) / rag_factory(44) / vector_store(230)
- 低覆盖：report_service 28%(274行) / report.py 32% / schedule.py 27% / publish.py 30% / version.py 40% / mock_data_gen 28% / simple_scheduler 52% / repositories 58% / file_store 28%
- 当前无认证；报告生成同步阻塞；数据源拉取无重试；知识库全量注入前 5 篇

## Q 阶段 — RBAC 权限

- Q1: User 实体（username/password_hash/role: admin|analyst|viewer/enabled）+ 建表 + 种子用户（admin/admin123, analyst/analyst123, viewer/viewer123，密码 bcrypt→用 hashlib.pbkdf2_hmac 免依赖）
- Q2: 认证中间件：/api/auth/login 签发 token（hmac 自签，带过期）+ Authorization: Bearer 校验依赖 get_current_user
- Q3: 权限依赖 require_role：admin=全部（含用户管理/数据源/知识库/选配写）；analyst=生成/审核/推送/任务；viewer=只读（list/detail/content/compare）
- Q4: 前端登录页 + token 存 localStorage + 路由守卫 + axios 拦截器注入 token + 403 处理

## R 阶段 — 性能

- R1: 报告生成异步化：/api/report/generate 创建 PENDING 任务立即返回 task_id，asyncio.create_task 后台跑；/api/report/status/{task_id} 轮询；前端生成按钮改轮询
- R2: 列表索引核对 + 版本内容查询 limit；指标缓存已有

## S 阶段 — 容错

- S1: 数据源拉取失败重试 2 次（指数退避，单源失败不阻塞整体已有）
- S2: 同窗口同周期 PENDING/RUNNING 幂等拒绝（防并发重复生成）
- S3: 全局异常处理器核对（ApiResponse 统一错误格式已有）

## T 阶段 — 高阶 AI

- T1: 知识库按指标关键词检索（rag_facade 增强：recall_for_metric 增加 KB 关键词匹配，替代全量注入）
- T2: 报告摘要：版本列表返回自动摘要（LLM 或规则截取——用规则：overview 章节前 120 字）
- T3: 风险预测：LLM 基于环比趋势预测下周期风险等级（趋势章节增强）

## U 阶段 — 覆盖率 ≥80%

- U1: 补 judge 测试（llm_factory/llm_judge/prompt_builder/prompt_manager：mock LLM 全链路 + 降级路径）
- U2: 补 rag/vector 测试（mock chromadb）
- U3: 补 report_service 全 pipeline 测试（mock adapter/judge/render）
- U4: 补 scheduler/simple_scheduler/mock_data_gen/repositories/file_store/publish/schedule/version 测试
- U5: 最终 80% 验证

## 验收

pytest --cov 全绿且 ≥80%；RBAC 三角色访问控制验证；异步生成轮询验证；重试日志验证；知识检索注入验证；前端登录+守卫验证
